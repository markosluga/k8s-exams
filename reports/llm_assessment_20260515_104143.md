# LLM Assessment Report

**Date:** 2026-05-15 10:41:43  
**Model:** claude-opus-4-7  
**Result:** PASS  

## Summary

| Status | Count |
|--------|-------|
| Clean — `llm_validated=1` | 59 |
| Fixed inline — `llm_validated=1` (report is audit trail) | 91 |
| Needs manual review — `llm_validated=2` | 0 |
| API failures — `llm_validated=0` (retryable) | 0 |
| **Total assessed** | **150** |

Errors: 0  Warnings: 0

## Clean Questions (llm_validated=1)

### kcsa-050

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. MITRE ATT&CK for Containers is explicitly a knowledge base documenting adversary tactics, techniques, and procedures (TTPs) for container/Kubernetes environments, per MITRE's official documentation.

**question_clarity**

The question is clear and directly asks the purpose of MITRE ATT&CK for Containers in a Kubernetes security context.

**distractor_quality**

Distractors A, C, and D represent plausible-sounding but distinct security/DevOps activities (benchmarking, compliance, vulnerability scanning) that someone unfamiliar might confuse with ATT&CK. They are clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes ATT&CK for Containers, listing real tactic categories and giving valid examples of techniques and detection mapping (e.g., Falco rules).

**difficulty_calibration**

Medium is appropriate — requires familiarity with security frameworks beyond beginner level but not deep specialization.

**domain_relevance**

Directly tests knowledge of the Kubernetes Threat Model domain, specifically threat modeling frameworks.

### kcsa-026

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Cryptomining malware after compromising a Kubernetes cluster overwhelmingly deploys pods/containers to consume CPU (and sometimes GPU) for mining cryptocurrency. This is well-documented in incidents like TeamTNT, Hildegard, and Kinsing campaigns.

**question_clarity**

The question is clear and unambiguous, asking about the most common post-compromise behavior of cryptomining malware in Kubernetes.

**distractor_quality**

Distractors are plausible Kubernetes attack vectors (etcd secret theft, image exfiltration, kube-proxy manipulation) but none align with cryptomining motives. They effectively test whether the learner understands the attacker's goal.

**explanation_accuracy**

The explanation is factually accurate: cryptominers deploy resource-heavy pods, Falco and resource quotas are standard detection/mitigation, and TeamTNT is a real threat actor targeting Kubernetes for mining.

**difficulty_calibration**

Medium is appropriate — it requires understanding both threat actor motivation and Kubernetes-specific attack patterns, beyond basic recall.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, covering attacker tactics against clusters.

### kcsa-056

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Kubernetes does not have a built-in registry allowlist; admission controllers (OPA Gatekeeper, Kyverno, ValidatingAdmissionPolicy, or custom webhooks) are the standard mechanism for enforcing image source restrictions.

**question_clarity**

The question is clear and unambiguous, asking for the correct enforcement mechanism for image registry restrictions.

**distractor_quality**

Distractors are plausible: imagePullPolicy is image-related but doesn't restrict sources; NetworkPolicy egress is a complementary control but DNS-based blocking is unreliable; ResourceQuota is unrelated to image sources but namespace-scoped quotas sound vaguely relevant.

**explanation_accuracy**

The explanation accurately describes how admission controllers inspect pod image fields and notes NetworkPolicy as complementary defense-in-depth. Technically accurate.

**difficulty_calibration**

Medium is appropriate — requires understanding of admission control mechanisms beyond basic Kubernetes concepts.

**domain_relevance**

Directly tests Platform Security knowledge regarding supply chain and admission control.

### kcsa-090

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The pods/exec subresource (via create verb) allows kubectl exec into containers, granting arbitrary command execution which is a well-known privilege escalation vector documented in Kubernetes RBAC security guidance.

**question_clarity**

The question clearly asks about the security implication of granting 'create' on 'pods/exec'. Phrasing is unambiguous and technically precise.

**distractor_quality**

Distractors are plausible misconceptions: A confuses exec with host access, C sounds like a reasonable misreading of 'exec', and D conflates kubelet exec with pod exec. All are clearly wrong to an expert but tempting to a novice.

**explanation_accuracy**

The explanation accurately describes that pods/exec allows shell access into containers, can expose secrets, enable privilege escalation (especially with privileged containers), and lateral movement. Matches official Kubernetes RBAC documentation.

**difficulty_calibration**

Hard is slightly aggressive but defensible — understanding subresource RBAC semantics and the 'create' verb mapping to exec is an advanced KCSA topic. Medium would also be reasonable but hard is acceptable.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically RBAC subresource permissions and their security implications.

### kcsa-043

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. With --anonymous-auth=true, unauthenticated requests are mapped to system:anonymous and system:unauthenticated group, which can be granted permissions via RBAC (a known risk, especially historically with system:unauthenticated bindings). Options A, C, and D are factually incorrect.

**question_clarity**

The question is clear: it asks about the security risk of enabling anonymous auth on the kube-apiserver. The phrasing is direct and unambiguous.

**distractor_quality**

Distractors are plausible Kubernetes-flavored statements that a learner might confuse with anonymous auth behavior (discovery endpoints, kubelet auth, audit logging), but each is clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes the system:anonymous user and system:unauthenticated group mapping and the RBAC risk. The recommendation to set --anonymous-auth=false is consistent with CIS Benchmarks. Minor nuance: /healthz, /livez, /readyz are accessible via AnonymousAuthConfiguration in newer versions, but the explanation's mention of healthz exception is acceptable.

**difficulty_calibration**

Medium is appropriate — requires knowing the specific user/group assignment and RBAC interaction, beyond simple definition recall.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security (kube-apiserver flag hardening), matching the stated domain.

### kcsa-141

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Setting readOnlyRootFilesystem: true makes the container's root filesystem read-only, while any explicitly mounted volumes (emptyDir, PVC, etc.) remain writable based on their own settings. This matches Kubernetes documentation on SecurityContext.

**question_clarity**

The question is clear and directly asks what the setting accomplishes. No ambiguity in phrasing.

**distractor_quality**

Distractors are plausible misconceptions: A confuses image layers with runtime filesystem, C and D conflate this setting with volume-specific access controls. All are clearly wrong to an expert but believable to a novice.

**explanation_accuracy**

Explanation is accurate: correctly notes root filesystem is read-only, mentions security benefits, and clarifies that writable volumes can be mounted for apps needing write access.

**difficulty_calibration**

Easy is appropriate — this is a single-concept definitional question about a well-known securityContext field.

**domain_relevance**

Directly tests Platform Security knowledge of pod/container security context hardening.

### kcsa-086

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct per Kubernetes documentation. The --rotate-certificates flag enables the kubelet to automatically rotate its client certificate by requesting new certs via the CertificateSigningRequest API before expiry.

**question_clarity**

Question is clear and direct, asking specifically what the flag enables.

**distractor_quality**

Distractors are plausible: pod TLS rotation, SA token rotation, and CA rotation are all related security/certificate concepts that could confuse learners but are clearly wrong to experts.

**explanation_accuracy**

Explanation accurately describes the CSR-based rotation mechanism and correctly mentions the companion --rotate-server-certificates flag for kubelet serving certs.

**difficulty_calibration**

Medium is appropriate — requires knowledge of kubelet flags and the CSR API beyond beginner level but is not deeply nuanced.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security (kubelet TLS bootstrapping and rotation).

### kcsa-014

**Per-dimension analysis:**

**answer_correctness**

RequestResponse is the correct audit level that logs both request and response bodies, per Kubernetes audit policy documentation. Answer D is definitively correct.

**question_clarity**

The question is clear and direct, asking which audit level records full request and response body.

**distractor_quality**

All four distractors are the actual audit levels in Kubernetes, making them highly plausible and well-calibrated.

**explanation_accuracy**

The explanation accurately describes all four levels and adds useful best-practice context about logging Secrets only at Metadata level.

**difficulty_calibration**

Medium is appropriate — requires knowing the four audit levels and their distinctions, not just a single definition.

**domain_relevance**

Audit logging is directly relevant to Kubernetes Cluster Component Security (API server auditing).

### kcsa-133

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. gVisor is an application kernel/sandbox developed by Google that intercepts syscalls in user space (via the Sentry component) and provides an isolation layer between the container and host kernel, matching official gVisor documentation.

**question_clarity**

The question clearly asks for both the definition of gVisor and the security problem it addresses. Phrasing is unambiguous.

**distractor_quality**

Distractors A, C, and D represent plausible but distinct categories of Kubernetes security tools (scanners, admission controllers, RBAC tools), all clearly wrong to someone who knows gVisor. They are well-constructed.

**explanation_accuracy**

The explanation accurately describes gVisor's user-space kernel approach, the Sentry-style syscall interposition, its use in GKE Sandbox via RuntimeClass, and correctly notes performance/compatibility trade-offs and Kata Containers as an alternative VM-based approach.

**difficulty_calibration**

Hard is slightly aggressive — recognizing gVisor as a sandbox runtime is more medium-level recall. However, given the depth implied (syscall interposition, trade-offs), hard is defensible.

**domain_relevance**

Directly relevant to Platform Security: sandboxed container runtimes are a core KCSA topic under workload isolation.

### kcsa-015

**Per-dimension analysis:**

**answer_correctness**

Admission controllers are indeed the components that validate and mutate API requests after authn/authz but before persistence to etcd. Answer C is definitively correct per Kubernetes documentation.

**question_clarity**

The question is clear and directly asks about the component responsible for validating and mutating API requests prior to etcd persistence.

**distractor_quality**

kube-scheduler, kube-proxy, and kube-controller-manager are all real Kubernetes components with unrelated responsibilities, making them plausible distractors for novices but clearly wrong to experts.

**explanation_accuracy**

The explanation correctly describes admission controller ordering (after authn/authz, before persistence) and provides accurate examples like LimitRanger, ResourceQuota, PodSecurity, and the webhook types.

**difficulty_calibration**

Easy is appropriate — this is a single-concept identification question about a well-known control plane mechanism.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, as admission control is a key security checkpoint in the API request flow.

### kcsa-122

**Per-dimension analysis:**

**answer_correctness**

Option B correctly defines cryptojacking as unauthorized use of cluster compute for cryptocurrency mining and lists valid detection/prevention controls. The other options either misdefine the term (A conflates with data theft, C with PKI attacks) or describe only one narrow attack vector (D). B is unambiguously the best answer.

**question_clarity**

The question is clear and asks two related things: definition and controls. The compound nature is acceptable since the answer options consistently pair both elements.

**distractor_quality**

Distractors are plausible: A plays on the word 'crypto' (encryption vs cryptocurrency), C invokes another crypto-related attack, and D describes a real attack vector for cryptojacking but with an incomplete definition. All are reasonable traps without being correct.

**explanation_accuracy**

The explanation accurately describes cryptojacking, lists realistic detection tools (Prometheus, Falco with miner process names like xmrig/minerd) and prevention controls (CPU limits, ResourceQuota, image signing). Consistent with CNCF security guidance.

**difficulty_calibration**

Medium is appropriate — requires recognizing a specific threat term and associated controls, which goes beyond basic recall but isn't deeply technical.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, covering a well-known cloud-native attack pattern.

### kcsa-012

**Per-dimension analysis:**

**answer_correctness**

The correct flag is --encryption-provider-config, which has been the stable flag name since Kubernetes 1.13 (previously --experimental-encryption-provider-config). Option B is definitively correct. The other options are fabricated flag names that don't exist.

**question_clarity**

The question is clear and direct: it asks which kube-apiserver flag enables encryption of Secrets at rest in etcd. No ambiguity.

**distractor_quality**

Distractors A, C, and D are plausible-sounding fabricated flag names that follow Kubernetes naming conventions, making them believable to someone who hasn't memorized the exact flag. They are clearly wrong to anyone familiar with the actual API server flags.

**explanation_accuracy**

The explanation correctly identifies the flag, mentions the EncryptionConfiguration resource, lists valid providers (aescbc, aesgcm, kms), and correctly notes that existing Secrets must be rewritten using kubectl get/replace to apply encryption. All factually accurate.

**difficulty_calibration**

Medium is appropriate. Recognizing the exact flag name requires hands-on familiarity with cluster hardening, which is beyond beginner level but not advanced.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security — specifically kube-apiserver hardening and etcd data-at-rest encryption, a core KCSA topic.

### kcsa-082

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly describes APF. APF classifies inbound requests into priority levels and uses fair queuing within each level to prevent any single client from starving others, protecting the API server from DoS. This matches official Kubernetes documentation.

**question_clarity**

The question clearly asks for both what APF is and what security problem it solves. The phrasing is unambiguous.

**distractor_quality**

Distractors are plausible misconceptions: A confuses 'priority' with security-call priority, C confuses with pod scheduling/PriorityClass, D confuses with RBAC authorization. All are clearly wrong to someone who knows APF but tempting to novices.

**explanation_accuracy**

APF reached GA/stable in Kubernetes 1.29, which is correct. The explanation accurately notes APF replaced the simple max-inflight throttling with FlowSchemas and PriorityLevelConfigurations. Mention of priority levels and queuing is accurate.

**difficulty_calibration**

Hard is appropriate — APF is an advanced API server topic typically not covered at introductory level, requiring understanding of FlowSchemas, PriorityLevelConfigurations, and DoS protection mechanisms.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, as APF is a kube-apiserver protection mechanism.

### kcsa-065

**Per-dimension analysis:**

**answer_correctness**

Answer B accurately describes ESO. It is a CNCF project that syncs secrets from external providers like AWS Secrets Manager, HashiCorp Vault, Azure Key Vault, GCP Secret Manager into native Kubernetes Secret objects via SecretStore/ClusterSecretStore and ExternalSecret CRDs.

**question_clarity**

The question is direct and unambiguous, asking for a definition of ESO.

**distractor_quality**

Distractors are plausible-sounding but clearly wrong: A confuses with etcd, C with cert-manager/krew plugin, D with OPA Gatekeeper. They test understanding of common adjacent tools.

**explanation_accuracy**

Explanation correctly identifies the CRDs (SecretStore, ExternalSecret) and the rationale for using ESO — keeping plaintext secrets out of Git while centralizing lifecycle in a dedicated vault. Note: secrets still land in etcd as Kubernetes Secrets, but the explanation hedges this with 'as primary storage,' which is acceptable.

**difficulty_calibration**

Medium is appropriate; ESO is a specialized but increasingly common tool that goes beyond beginner Kubernetes knowledge.

**domain_relevance**

Fits the Overview of Cloud Native Security domain as secrets management is a core cloud-native security concern.

### kcsa-097

**Per-dimension analysis:**

**answer_correctness**

Answer B accurately describes a LimitRange per Kubernetes documentation: it's a namespace-scoped policy that enforces min/max/default resource constraints for pods, containers, and PVCs. The security contribution (preventing resource exhaustion/DoS) is also correctly identified. No other option is a valid description of LimitRange.

**question_clarity**

The question is clear and asks two related things: what a LimitRange is and how it contributes to security. The stem is grammatically sound and unambiguous.

**distractor_quality**

Distractors are plausible misconceptions: A confuses LimitRange with NetworkPolicy, C with RBAC, D with ResourceQuota. All are clearly wrong to someone who knows the concept but tempt the unprepared.

**explanation_accuracy**

The explanation correctly describes LimitRange's role in setting defaults and bounds, and properly distinguishes it from ResourceQuota. The DoS framing is accurate — without limits, a single container can exhaust node resources. Technically LimitRange also covers PVC storage and ephemeral storage, but the CPU/memory focus is correct and sufficient.

**difficulty_calibration**

Medium is appropriate — the concept requires understanding the distinction between LimitRange, ResourceQuota, NetworkPolicy, and RBAC, plus the security implication, which goes beyond a pure definition.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals, as resource limits are a recognized defense against denial-of-service and noisy-neighbor attacks.

### kcsa-127

**Per-dimension analysis:**

**answer_correctness**

Lateral Movement is the correct tactic. MITRE ATT&CK for Containers explicitly lists 'Use Cloud Credentials' and similar techniques under Lateral Movement (T1550 variants), and Microsoft's Kubernetes threat matrix lists 'Access cloud resources' and 'Access Kubernetes service account' under Lateral Movement. Pivoting using a stolen SA token clearly fits this tactic.

**question_clarity**

The stem clearly asks which MITRE ATT&CK for Containers tactic covers using a compromised ServiceAccount token to pivot to other cluster services. The scenario (pivoting) maps cleanly to one tactic.

**distractor_quality**

Initial Access, Persistence, and Defense Evasion are all valid MITRE tactics and plausible misclassifications for someone confusing the kill-chain stages. None are correct here, making them effective distractors.

**explanation_accuracy**

The explanation correctly attributes the classification to both Microsoft's Kubernetes Threat Matrix and MITRE ATT&CK for Containers, and the suggested mitigations (RBAC least privilege, audience-bound tokens, NetworkPolicy) are accurate.

**difficulty_calibration**

Hard is reasonable since it requires familiarity with the MITRE ATT&CK for Containers matrix specifically, not just general Kubernetes security concepts. Medium would also be defensible but hard is acceptable.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain by mapping a concrete attack scenario to the MITRE ATT&CK tactic taxonomy.

### kcsa-114

**Per-dimension analysis:**

**answer_correctness**

B is definitively correct. Container image poisoning refers to a supply chain attack where attackers compromise images in a registry or build pipeline to inject malicious code before deployment. This aligns with CNCF supply chain security guidance and the Kubernetes threat model.

**question_clarity**

The question is clear and directly asks for the definition of container image poisoning as a supply chain attack. No ambiguity.

**distractor_quality**

Distractors are plausible: A is a hardware supply chain attack (different layer), C describes runtime injection, D describes node-level cache corruption. All are wrong but believable to a novice.

**explanation_accuracy**

Explanation accurately describes the attack and provides correct defenses including digest pinning, Cosign signature verification, and registry scanning. All recommendations align with current best practices.

**difficulty_calibration**

Marked hard, but this is closer to medium since it's a single-concept definition question. However, the nuance of distinguishing supply chain layers could justify hard. Borderline but acceptable.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, specifically the supply chain compromise threat category.

### kcsa-053

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. As of Kubernetes 1.30, AppArmor profiles are configured via the appArmorProfile field within securityContext (either at pod or container level), with the three valid types: RuntimeDefault, Localhost, and Unconfined. This matches official Kubernetes documentation.

**question_clarity**

The question is clear and specific, asking about the GA configuration method since 1.30. It distinguishes from the deprecated annotation-based approach implicitly through the 'since GA' framing.

**distractor_quality**

Distractors are plausible: B (DaemonSet) reflects real-world approaches for loading profiles on nodes; C (ConfigMap) is a common Kubernetes pattern; D (kubelet flag) sounds like a reasonable configuration mechanism. All are clearly wrong to someone who knows the GA spec.

**explanation_accuracy**

The explanation accurately describes the appArmorProfile field, lists the three valid types correctly, mentions the deprecated annotation path, and correctly characterizes AppArmor's scope (file access, capabilities, network).

**difficulty_calibration**

Hard is appropriate given this requires knowledge of recent GA changes (1.30), the specific field name, and the three profile types — not a definitional concept.

**domain_relevance**

Directly tests Platform Security knowledge regarding workload hardening via AppArmor, a core KCSA topic.

### kcsa-019

**Per-dimension analysis:**

**answer_correctness**

Option B is definitively correct. The standard Kubernetes pattern for least-privilege capabilities is `capabilities: { drop: ["ALL"], add: ["NET_BIND_SERVICE"] }`, which matches the question stem exactly. No other option drops all and selectively adds back.

**question_clarity**

The question is clear and unambiguous, asking specifically about the securityContext pattern that drops all caps and re-adds selectively. The phrasing maps directly to the correct option.

**distractor_quality**

Distractors are reasonably plausible: A only adds ALL (opposite intent), C uses a non-existent 'default' keyword but looks plausible, D is a related but distinct securityContext field. Each tests a real misconception about capability management.

**explanation_accuracy**

The explanation correctly describes drop:[ALL] + add:[NET_BIND_SERVICE], correctly notes ports <1024 binding rationale, and accurately references combining with allowPrivilegeEscalation:false. All technically accurate.

**difficulty_calibration**

Labeling this 'hard' is a stretch — recognizing the drop-all-then-add pattern is a fairly common CKS/KCSA concept and most candidates aware of capabilities know this idiom. 'Medium' would fit better, but it's not a glaring mismatch given KCSA's scope where capability syntax is less commonly emphasized than in CKS.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals — pod securityContext and Linux capabilities are core KCSA topics.

### kcsa-142

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Kubernetes has no native image registry allowlist; admission controllers (OPA Gatekeeper, ValidatingAdmissionPolicy with CEL, or custom webhooks) are the standard mechanism to enforce registry allowlists by inspecting the image field at admission time.

**question_clarity**

The question clearly asks for the correct mechanism to restrict image registries. Unambiguous and well-scoped.

**distractor_quality**

Distractors are plausible: A is a common misconception (imagePullPolicy controls caching, not source), C confuses network-level controls with admission control (and blocking DNS isn't a proper allowlist mechanism), and D is a misuse of ResourceQuota. All are clearly wrong to an expert.

**explanation_accuracy**

Explanation accurately describes the absence of a built-in field and correctly lists OPA Gatekeeper, ValidatingAdmissionPolicy (CEL), and ValidatingAdmissionWebhook as enforcement options. The note about NetworkPolicy complementing is reasonable.

**difficulty_calibration**

Medium is appropriate — requires knowing admission controller landscape beyond basics but isn't deeply complex.

**domain_relevance**

Directly relevant to Platform Security (supply chain / image provenance controls).

### kcsa-033

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. OPA Gatekeeper is a CNCF project that integrates Open Policy Agent with Kubernetes via a ValidatingAdmissionWebhook (and now also a MutatingAdmissionWebhook) to enforce custom policies written in Rego on resource creation and updates.

**question_clarity**

The question is clear and direct, asking about the purpose of OPA Gatekeeper in a Kubernetes cluster.

**distractor_quality**

Distractors are plausible security-adjacent functions (KMS, image scanning, LDAP auth) that someone unfamiliar with Gatekeeper might confuse with it, but each is clearly a different tool category to experts.

**explanation_accuracy**

The explanation accurately describes ConstraintTemplates and Constraints, the use of Rego, and provides representative policy examples. All technically accurate.

**difficulty_calibration**

Labeled hard, but this is arguably medium since it tests recognition of Gatekeeper's purpose rather than nuanced policy authoring. However, given KCSA context and the depth of policy-as-code knowledge, hard is defensible — not a clear mismatch.

**domain_relevance**

Directly tests Platform Security domain knowledge (admission control, policy enforcement).

### kcsa-013

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The --use-service-account-credentials flag in kube-controller-manager causes each controller loop to run with its own dedicated ServiceAccount (e.g., deployment-controller, replicaset-controller) rather than sharing the kube-controller-manager's credentials, implementing least privilege as documented in the Kubernetes docs.

**question_clarity**

The question is clear and directly asks what the specified flag does. No ambiguity in phrasing.

**distractor_quality**

Distractors are plausible and relate to ServiceAccount concepts: A confuses pod-side behavior, C references the automountServiceAccountToken concept, D references the BoundServiceAccountTokenVolume/projected token feature. All are clearly wrong to an expert but tempting for novices.

**explanation_accuracy**

The explanation accurately describes the effect: per-controller ServiceAccounts (deployment-controller, etc.) with scoped RBAC instead of the shared controller-manager credential. This matches official documentation.

**difficulty_calibration**

Hard is appropriate — this is an obscure controller-manager flag tested in CKS/KCSA contexts requiring deep knowledge of control plane hardening.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically control plane component hardening flags.

### kcsa-123

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The cluster autoscaler does require elevated cloud IAM permissions (e.g., to manage node groups, create/delete instances), and a common attack pattern is stealing those credentials via SSRF or metadata service access from a pod scheduled on the autoscaler's node. The other options describe operational issues or denial-of-service patterns, not attacks specifically targeting the autoscaler's cloud permissions.

**question_clarity**

The question is clear and focused: it asks which attack targets the autoscaler's cloud provider permissions. The phrasing unambiguously points to credential/permission theft rather than abuse of scaling behavior.

**distractor_quality**

Distractors are plausible scenarios related to autoscaling (filling nodes, metric poisoning, cost exhaustion) but none target the IAM credentials themselves, making B clearly the correct answer to an informed reader while remaining tempting to novices.

**explanation_accuracy**

The explanation accurately describes the threat: autoscaler IAM role compromise via metadata service (169.254.169.254) SSRF, and recommends correct mitigations (IRSA/Workload Identity, NetworkPolicy blocking metadata access). All technical details align with AWS/GCP documentation and CNCF guidance.

**difficulty_calibration**

Hard is appropriate. The question requires understanding cluster autoscaler architecture, cloud IAM, instance metadata services, and SSRF attack patterns — multi-domain knowledge typical of advanced KCSA/CKS material.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain by examining a specific attack vector against a cluster component (autoscaler) and its trust boundary with the cloud provider.

### kcsa-038

**Per-dimension analysis:**

**answer_correctness**

Trivy is correctly identified as the CNCF-hosted container vulnerability scanner among the options. While the phrase 'CNCF standard' is imprecise (CNCF does not designate official 'standards'), Trivy is the most prominent CNCF project for image vulnerability scanning and is unambiguously the best answer here since Falco (runtime), Cosign (signing), and Kube-bench (CIS benchmarks) serve entirely different functions.

**question_clarity**

The question is short and clear in intent. The phrasing 'CNCF standard' is slightly loose terminology since CNCF hosts projects rather than declaring standards, but the meaning ('CNCF-hosted vulnerability scanner') is easily inferred and does not cause real ambiguity among the four options.

**distractor_quality**

The distractors are well chosen — all are real, well-known CNCF/security ecosystem tools (Falco for runtime detection, Cosign for signing, Kube-bench for CIS compliance). A learner who confuses security tooling categories could plausibly pick any of them, making them effective distractors.

**explanation_accuracy**

The explanation accurately describes Trivy's capabilities (CVEs, misconfigurations, secrets, SBOMs, OS and language packages) and correctly differentiates the other tools. Trivy is indeed a CNCF Incubating project (promoted from Sandbox in 2024), so the parenthetical is accurate.

**difficulty_calibration**

Medium is reasonable. While recognizing Trivy is fairly straightforward, distinguishing the purpose of four overlapping security tools (Falco, Cosign, Kube-bench, Trivy) requires more than entry-level knowledge.

**domain_relevance**

Directly relevant to Platform Security, which covers supply chain and image scanning tools used to secure the Kubernetes platform.

### kcsa-021

**Per-dimension analysis:**

**answer_correctness**

B is correct. BoundServiceAccountTokenVolume creates projected tokens that are time-bound (expiration), audience-bound, and object-bound (tied to the pod), replacing the legacy non-expiring Secret-based tokens. This matches official Kubernetes documentation.

**question_clarity**

The question clearly asks the purpose of the projected ServiceAccount token feature, with the parenthetical naming the feature gate/mechanism for context. Unambiguous.

**distractor_quality**

A misuses 'projected' (sounding plausible but wrong), C plays on token security assumptions, and D suggests external use. All are plausible misconceptions but clearly incorrect to those who know the feature.

**explanation_accuracy**

Explanation is accurate: projected tokens are short-lived, audience-bound, and rotated by kubelet. Minor note: BoundServiceAccountTokenVolume became GA in 1.22 and was enabled by default earlier, but the explanation's core technical claims are correct.

**difficulty_calibration**

Hard is reasonable — this requires knowing the specific feature gate name and its semantics (audience binding, rotation), which is advanced KCSA-level knowledge.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals, specifically ServiceAccount token security improvements.

### kcsa-054

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. kube-bench is an open-source tool from Aqua Security that checks Kubernetes deployments against the CIS Kubernetes Benchmark. The other options describe performance/load testing tools, which kube-bench is not.

**question_clarity**

The question is direct and unambiguous: it asks for the purpose of kube-bench, which has a single well-known answer.

**distractor_quality**

Distractors are plausible because they all relate to Kubernetes tooling and the word 'bench' could suggest benchmarking/performance testing. They are clearly wrong to anyone familiar with kube-bench.

**explanation_accuracy**

The explanation accurately describes kube-bench: maintained by Aqua Security, runs CIS Kubernetes Benchmark checks, inspects API server/etcd/kubelet/RBAC, can run as a Job or directly on nodes, and is used for compliance audits.

**difficulty_calibration**

Easy is appropriate—this is a single-concept tool identification question commonly covered in KCSA preparation.

**domain_relevance**

Directly relevant to Compliance and Security Frameworks domain, as CIS Benchmarks are a core compliance framework for Kubernetes.

### kcsa-031

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. Standard TLS authenticates only the server to the client; mTLS adds client authentication via client certificates, providing mutual authentication. This is the textbook definition.

**question_clarity**

The question is clear and directly asks what mTLS adds over standard TLS. No ambiguity in phrasing.

**distractor_quality**

Distractors are plausible misconceptions: A confuses TLS performance characteristics, C confuses mTLS with general encryption scope, D is a related but separate feature. All are clearly wrong to an expert but tempting to a novice.

**explanation_accuracy**

Explanation correctly describes that standard TLS authenticates only the server while mTLS requires both parties to present certificates. The service mesh context (Istio, Linkerd, Zero Trust) is accurate and relevant to KCSA.

**difficulty_calibration**

Medium is appropriate. It requires understanding of TLS handshake mechanics beyond basic definitions but is not deeply nuanced.

**domain_relevance**

Directly relevant to Platform Security, particularly service mesh and Zero Trust networking concepts in KCSA.

### kcsa-016

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct per official Kubernetes RBAC documentation: Role is namespace-scoped while ClusterRole is cluster-scoped. This is the fundamental distinction between the two resource types.

**question_clarity**

The question is clear and directly asks about the difference between Role and ClusterRole, a well-defined RBAC concept.

**distractor_quality**

All three distractors are plausible misconceptions: A inverts the relationship, C invents a subject-based distinction, and D fabricates a resource-type distinction. None are obviously wrong to a novice but all are clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes both scopes and correctly notes the nuance that a RoleBinding can reference a ClusterRole to grant namespaced permissions, which is an important and correct detail from the official docs.

**difficulty_calibration**

Easy is appropriate — this is a foundational RBAC definition question with a single concept being tested.

**domain_relevance**

RBAC Role vs ClusterRole is core to Kubernetes Security Fundamentals, the stated domain.

### kcsa-039

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. The CIS Kubernetes Benchmark is a widely recognized prescriptive guide published by the Center for Internet Security providing configuration hardening recommendations for Kubernetes components. No other option is correct.

**question_clarity**

The question is clear and direct, asking what the CIS Kubernetes Benchmark provides. There is no ambiguity.

**distractor_quality**

Distractors are plausible Kubernetes-adjacent security topics (IDS, PKI/CA, patch management) that someone unfamiliar with CIS could confuse but are clearly wrong to those who know CIS Benchmarks are configuration guidance documents.

**explanation_accuracy**

The explanation is accurate: CIS Benchmarks are prescriptive hardening guides; the Kubernetes Benchmark covers API server, etcd, kubelet, RBAC; kube-bench automates checks; and CIS is referenced by compliance frameworks like SOC2 and PCI-DSS.

**difficulty_calibration**

Medium is appropriate. It requires familiarity with the compliance/security tooling landscape beyond basic Kubernetes knowledge, though it's a recognition-level question.

**domain_relevance**

Directly tests knowledge of compliance and security frameworks, specifically the CIS Kubernetes Benchmark, which is core to the stated domain.

### kcsa-040

**Per-dimension analysis:**

**answer_correctness**

Answer B accurately reflects NIST SP 800-190 recommendations: minimal base images, vulnerability scanning, and immutable references (pinning to digests). These are explicitly covered in sections on image countermeasures.

**question_clarity**

The question clearly asks about NIST SP 800-190 recommendations for container images. The stem is unambiguous and properly contextualized.

**distractor_quality**

All three distractors represent realistic anti-patterns: using 'latest' tag (A), bloated base images (C), and disabling scans (D). Each is plausible to a novice but clearly wrong to anyone familiar with container security.

**explanation_accuracy**

The explanation correctly summarizes NIST SP 800-190's image-related recommendations including minimal images, scanning, signing, and immutable references. Distroless/Alpine examples are accurate.

**difficulty_calibration**

Hard is reasonable given the specificity of citing NIST SP 800-190; however, the answer is identifiable by general security best practices alone, so medium would also be defensible. Not a clear mismatch.

**domain_relevance**

Directly tests knowledge of a compliance/security framework (NIST SP 800-190), which fits the Compliance and Security Frameworks domain.

### kcsa-017

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The field spec.automountServiceAccountToken: false in a Pod spec disables automatic mounting of the ServiceAccount token, as documented in the official Kubernetes documentation on ServiceAccounts.

**question_clarity**

The question is clear and unambiguous, asking specifically about the Pod spec field that disables automatic token mounting.

**distractor_quality**

The distractors are plausible-sounding fabricated field names (disableServiceAccount, serviceAccount.mount, serviceAccountName: none) that someone unfamiliar with the actual API would find believable but are clearly invalid to an expert.

**explanation_accuracy**

The explanation correctly describes the mount path (/var/run/secrets/kubernetes.io/serviceaccount/) and accurately notes the field can be set at either the ServiceAccount or Pod level, with the Pod-level setting taking precedence.

**difficulty_calibration**

Medium difficulty is appropriate — this requires knowing the specific field name and its security implications, beyond basic recall but not deeply nuanced.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals, specifically ServiceAccount token hardening, a core KCSA topic.

### kcsa-044

**Per-dimension analysis:**

**answer_correctness**

--client-ca-file is correct. It enables X.509 client certificate authentication by specifying the CA bundle used to verify client certificates presented to the API server, per official kube-apiserver documentation.

**question_clarity**

The question is clear and specific, asking which flag enables X.509 client certificate authentication.

**distractor_quality**

All distractors are plausible API server flags: --tls-cert-file (server TLS), --token-auth-file (static token auth), --oidc-issuer-url (OIDC auth). Each represents a different authentication or TLS concept, making them effective distractors.

**explanation_accuracy**

Explanation is accurate: CN maps to username, O maps to group, and kubelets/controller-manager use this mechanism. Correctly contrasts with --tls-cert-file which is for server identity.

**difficulty_calibration**

Medium is appropriate — requires distinguishing among several API server flags and understanding the cert auth mechanism.

**domain_relevance**

Directly tests Kubernetes cluster component security (API server authentication configuration).

### kcsa-091

**Per-dimension analysis:**

**answer_correctness**

The stated answer B is correct per Kubernetes documentation: --enable-admission-plugins adds plugins to the default enabled set rather than replacing it. The docs explicitly state 'the order in which the flag is specified does not matter' and the flag supplements defaults. Option A describes pre-1.10 behavior of the deprecated --admission-control flag.

**question_clarity**

The question is clear and directly asks about the behavior of the flag. The phrasing 'when adding plugins' slightly hints at the answer but is still appropriate context.

**distractor_quality**

Distractors are plausible: A reflects the older --admission-control behavior, C is a common misconception about flag order determining execution, and D is a reasonable-sounding but fabricated namespace-scoping idea. All are clearly distinguishable from the correct answer.

**explanation_accuracy**

The explanation correctly states the flag adds to the default list and notes --disable-admission-plugins as the counterpart. The list of default plugins (NodeRestriction, PodSecurity, ResourceQuota, LimitRanger) is accurate for modern Kubernetes versions.

**difficulty_calibration**

Medium is appropriate—this requires knowing the nuance that the flag supplements rather than replaces defaults, which is a common point of confusion but not deeply complex.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, as admission control is a core security mechanism of the API server.

### kcsa-156

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. kube-bench is an open-source tool from Aqua Security that automates checks against the CIS Kubernetes Benchmark, exactly as stated.

**question_clarity**

The question clearly asks two things: the purpose of kube-bench and which benchmark it implements. Option B addresses both parts directly.

**distractor_quality**

Distractors A, C, and D all reference plausible-sounding Kubernetes performance/testing concepts (API throughput, load testing, etcd latency) that are clearly wrong to someone who knows kube-bench but plausible to a novice.

**explanation_accuracy**

The explanation accurately describes kube-bench's origin, scope (API server, etcd, kubelet, RBAC, scheduler), execution modes (Job or directly on nodes), and compliance use cases. All factually correct.

**difficulty_calibration**

Easy is appropriate — this is a single-concept identification question about a well-known CNCF-adjacent tool.

**domain_relevance**

Directly tests Compliance and Security Frameworks domain knowledge, specifically the CIS Kubernetes Benchmark and automated compliance tooling.

### kcsa-069

**Per-dimension analysis:**

**answer_correctness**

The NSA (National Security Agency) co-published the Kubernetes Hardening Guide with CISA, first released in August 2021 and updated in August 2022. Answer B is definitively correct.

**question_clarity**

The question is clear and unambiguous, directly asking which agency partnered with CISA on the Kubernetes Hardening Guide.

**distractor_quality**

All distractors are plausible US government agencies that could reasonably be associated with cybersecurity guidance (FBI, NIST, DHS). NIST is particularly strong as a distractor given its security framework publications.

**explanation_accuracy**

The explanation accurately describes the guide's contents (pod security, network separation, auth, audit logging, upgrades) and correctly contextualizes it relative to CIS Benchmark. The FedRAMP reference is reasonable.

**difficulty_calibration**

Medium is appropriate — this requires knowledge of specific industry publications rather than core Kubernetes concepts, but isn't deeply nuanced.

**domain_relevance**

Directly relevant to Overview of Cloud Native Security as the NSA/CISA guide is a foundational security reference for Kubernetes.

### kcsa-146

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. A default deny-all NetworkPolicy uses an empty podSelector ({}) to match all pods in the namespace, combined with policyTypes specifying Ingress and/or Egress with no rules defined, which denies all matching traffic. This matches the official Kubernetes documentation.

**question_clarity**

The question is clear and asks for both the definition and mechanism of a deny-all NetworkPolicy. The options are distinct enough to evaluate.

**distractor_quality**

Distractors are plausible: B suggests a cluster-level resource (NetworkPolicy is namespaced), C invents an 'action: Deny' field (Kubernetes NetworkPolicy has no explicit deny action—Calico does), and D misuses namespaceSelector. All are believable to a novice but clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes the structure: empty podSelector plus policyTypes [Ingress, Egress] with no rules. It correctly notes this isolates pods and recommends the deny-first best practice. Technically accurate per Kubernetes docs.

**difficulty_calibration**

Medium is appropriate—requires understanding NetworkPolicy semantics, the empty selector behavior, and policyTypes, which is beyond a beginner-level concept.

**domain_relevance**

Directly tests Platform Security knowledge regarding network policies, a core security control in Kubernetes.

### kcsa-085

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The CIS Kubernetes Benchmark explicitly recommends 0600 permissions for kubeconfig files (e.g., /etc/kubernetes/admin.conf), and avoiding version control commits plus preferring short-lived OIDC tokens are well-established practices.

**question_clarity**

The question clearly asks about both file permissions and handling practices for kubeconfig files, which maps directly to option B.

**distractor_quality**

Distractors are plausible misconceptions: A confuses authentication with service mesh, C invents auto-rotation, D invents OS keychain encryption. All are clearly wrong to an expert but believable to a novice.

**explanation_accuracy**

Explanation accurately describes credential contents, 0600 permissions, gitignore practice, OIDC tokens, and admin kubeconfig risk. CIS Benchmark 1.1.13 specifies 0600 for admin.conf.

**difficulty_calibration**

Easy is appropriate — this is a single-concept best-practices recall question.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically credential file hygiene.

### kcsa-049

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. ValidatingAdmissionPolicy reached GA in Kubernetes 1.30 and provides an in-tree admission control mechanism using CEL expressions, eliminating the need for external webhook servers. This aligns with official Kubernetes documentation.

**question_clarity**

The question is clear and specific, asking for the definition of VAP with a precise version reference (1.30 GA). The stem unambiguously requests identification of what VAP is.

**distractor_quality**

Distractors are plausible and well-crafted: A confuses VAP with NetworkPolicy (both use CEL in newer features), C confuses it with RBAC, D conflates it with Falco runtime security. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

The explanation is factually accurate. VAP did reach GA in 1.30, uses CEL, runs in-tree (in kube-apiserver), and the listed use cases (label requirements, image registry restrictions, naming, resource limits) are standard VAP applications. Mention of OPA Gatekeeper and Kyverno as alternatives is correct.

**difficulty_calibration**

Hard is appropriate given this requires knowledge of a relatively new Kubernetes feature, its GA version, CEL, and how it differs from webhook-based admission controllers like Gatekeeper/Kyverno.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security — admission control is a core security mechanism at the API server level.

### kcsa-102

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The namespaceSelector field in NetworkPolicy ingress/egress rules selects namespaces by label, allowing/denying traffic from/to pods in those matching namespaces. This aligns with the official Kubernetes NetworkPolicy spec.

**question_clarity**

The question is clear, asking for both definition and use case of namespaceSelector. Phrasing is grammatically sound and unambiguous.

**distractor_quality**

Distractors are plausible: A confuses namespaceSelector with pod scheduling constraints, C invokes RBAC, and D references PodSecurity admission labels — all common areas of confusion for learners. None are correct.

**explanation_accuracy**

The explanation accurately describes namespaceSelector behavior, the label-based matching, the combination with podSelector for finer scoping, and the default same-namespace scoping when omitted. Consistent with Kubernetes documentation.

**difficulty_calibration**

Medium is appropriate; recognizing the precise semantics of namespaceSelector versus podSelector and default scoping requires more than introductory NetworkPolicy knowledge.

**domain_relevance**

NetworkPolicy is a core Kubernetes security primitive, directly relevant to the Kubernetes Security Fundamentals domain of KCSA.

### kcsa-023

**Per-dimension analysis:**

**answer_correctness**

B is correct. `kubectl auth can-i --list --as=<user>` is the documented way to list all permissions for a specified user via impersonation. A, C, and D are not real kubectl commands or subresources.

**question_clarity**

The question is clear and direct, asking which command shows allowed actions for a user.

**distractor_quality**

Distractors are plausible-sounding fake commands that a beginner might guess (check-permissions, get rbac, describe role <user>), but clearly wrong to anyone familiar with kubectl.

**explanation_accuracy**

The explanation correctly describes the --list flag, the specific-action variant, and --as-group for group impersonation. All accurate per official kubectl docs.

**difficulty_calibration**

Easy is appropriate — this is recognition of a single command.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals (RBAC auditing via impersonation).

### kcsa-052

**Per-dimension analysis:**

**answer_correctness**

Lateral Movement is the correct tactic for using compromised ServiceAccount tokens to pivot to other cluster resources/services. Microsoft's Threat Matrix for Kubernetes explicitly lists 'Access cloud resources' and 'Access Kubernetes service account' (or similar) under Lateral Movement / Credential Access, with using SA tokens to reach other services being a canonical Lateral Movement technique. Answer C is correct.

**question_clarity**

The question is clear: it asks which tactic covers using a compromised pod's SA token to call other cluster services. The phrasing unambiguously points to lateral movement within the cluster.

**distractor_quality**

Initial Access, Persistence, and Defense Evasion are all real tactics in the matrix and are plausible to someone unsure about the boundary between these categories (e.g., one could mistakenly think reusing a token is persistence). Distractors are well chosen.

**explanation_accuracy**

The explanation correctly attributes the matrix to Microsoft, correctly identifies Lateral Movement as covering pivoting via credentials, and offers accurate mitigations (RBAC least privilege, audience-bound/projected tokens, NetworkPolicy). No factual issues.

**difficulty_calibration**

Hard is reasonable since it requires knowing the specific Microsoft Threat Matrix taxonomy and distinguishing between tactics that overlap conceptually (e.g., Credential Access vs Lateral Movement). Medium would also be defensible but hard is acceptable.

**domain_relevance**

Directly tests Kubernetes Threat Model knowledge, specifically the Microsoft Threat Matrix tactics, which is core to the KCSA Threat Model domain.

### kcsa-115

**Per-dimension analysis:**

**answer_correctness**

Option B accurately describes a TOCTOU race in admission control: an object passes validation at creation time but is mutated afterward to a malicious state, exploiting webhooks that only validate CREATE. This is a well-documented Kubernetes security concern. The other options describe different concurrency issues (ConfigMap writes, scheduler races) or supply-chain timing gaps (D), not classical TOCTOU in admission.

**question_clarity**

The question is clear and specific, asking for the definition of TOCTOU in the context of Kubernetes admission control. No ambiguity in phrasing.

**distractor_quality**

Distractor A (ConfigMap race) is plausible-sounding but unrelated to admission. C invokes scheduler/admission interplay which sounds technical. D is actually a strong distractor since image-scan-to-deploy gaps are sometimes loosely called TOCTOU in supply-chain contexts — plausible but distinguishable from admission-control TOCTOU as framed by the question.

**explanation_accuracy**

The explanation correctly identifies the CREATE-only webhook pitfall and recommends covering both CREATE and UPDATE operations. Mentioning ValidatingAdmissionPolicy as a mitigation is accurate. Technically accurate overall.

**difficulty_calibration**

Hard is appropriate — TOCTOU in admission control is a nuanced security concept requiring understanding of webhook operation scoping and reconciliation behavior, beyond introductory KCSA material.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, covering admission-time attack vectors and webhook configuration weaknesses.

### kcsa-035

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. Falco is a CNCF runtime security tool that monitors kernel syscalls via eBPF or a kernel module to detect anomalous behavior at runtime. The other options describe different tools: kubesec/kube-linter (A), Trivy/Clair (C), and audit2rbac (D).

**question_clarity**

The question is clear and direct, asking specifically about Falco's role in a Kubernetes security stack. No ambiguity.

**distractor_quality**

All three distractors represent legitimate security activities in the Kubernetes ecosystem (manifest scanning, image scanning, RBAC tooling), making them plausible for someone unfamiliar with Falco specifically but clearly wrong to those who know the tool.

**explanation_accuracy**

The explanation accurately describes Falco's syscall monitoring via eBPF/kernel module, its real-time detection capabilities, YAML-based rules, and its complementary relationship with static tools. All factual claims align with Falco documentation.

**difficulty_calibration**

Labeled hard, but this is arguably medium since it's a single-concept recognition question about a well-known CNCF tool. However, Falco's specifics may be less familiar to KCSA candidates, so hard is defensible — not a clear mismatch.

**domain_relevance**

Directly tests Platform Security knowledge, specifically runtime security tooling which is a core KCSA topic.

### kcsa-027

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly describes the confused deputy pattern: a higher-privileged entity is tricked into misusing its authority on behalf of a less-privileged party. This aligns with the classic Hardy (1988) definition adapted to Kubernetes service accounts. The other options describe unrelated attacks.

**question_clarity**

The question is clear and specific, asking for the definition of a known security concept in the Kubernetes service account context.

**distractor_quality**

Distractors are plausible Kubernetes security-sounding scenarios (node impersonation, webhook denial, etcd staleness) that would tempt someone unfamiliar with the confused deputy concept but are clearly distinct attack types.

**explanation_accuracy**

The explanation accurately captures the confused deputy concept and provides a relevant Kubernetes example with reasonable mitigations (audience-bound tokens via TokenRequest API, RBAC least privilege).

**difficulty_calibration**

Hard is appropriate; the confused deputy term is a specialized security concept not commonly known outside security practitioners, fitting KCSA threat model depth.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, covering service account privilege abuse scenarios.

### kcsa-064

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. Defense in Depth is a well-established security principle meaning multiple layered/overlapping controls so a single failure doesn't lead to total compromise. This aligns with NIST and CNCF security guidance.

**question_clarity**

The question is clear and direct, asking for the meaning of a specific security term in the Kubernetes context.

**distractor_quality**

Distractors are plausible to novices: A plays on 'deep' as deep learning, C plays on 'depth' with resource limits, D plays on 'layers' with container image encryption. All are clearly wrong to an expert but tempting to a novice.

**explanation_accuracy**

The explanation accurately defines Defense in Depth and provides concrete Kubernetes examples (NetworkPolicy, RBAC, admission control, Falco, etcd encryption, audit logging) that legitimately represent independent layers.

**difficulty_calibration**

Medium is appropriate—slightly above easy because it requires understanding both the concept and its Kubernetes application, but not deeply technical.

**domain_relevance**

Directly tests a core concept within 'Overview of Cloud Native Security'.

### kcsa-120

**Per-dimension analysis:**

**answer_correctness**

Answer B accurately describes the well-known 'mutating webhook backdoor' attack pattern documented in security research (e.g., by Rory McCune and others). A malicious MutatingAdmissionWebhook can silently mutate pods cluster-wide to inject sidecars or modify specs. The other options describe either implausible or different attack vectors.

**question_clarity**

The question is clear and directly asks for the definition of a specific named attack pattern. The quoted term frames the scope appropriately.

**distractor_quality**

Distractors are plausible at first glance — they all involve webhooks and security concerns — but each is technically incorrect: webhooks don't control LoadBalancer IPs (A), TLS cert modification is a different attack (C), and PodSecurity bypass via webhook is conceptually muddled (D). They serve as reasonable lures.

**explanation_accuracy**

The explanation correctly identifies the attack mechanism, impact (invisible sidecar injection), and mitigations (RBAC on MutatingWebhookConfiguration, audit logs, webhook server integrity). All factually accurate.

**difficulty_calibration**

Hard is appropriate — this requires understanding of admission controllers, webhook architecture, and advanced threat modeling beyond basic Kubernetes operation.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain by addressing a specific cluster-wide attack vector and persistence mechanism.

### kcsa-149

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. It accurately summarizes PCI-DSS requirements as they apply to containerized environments handling cardholder data, including isolation, encryption, vulnerability scanning, audit logs, and least-privilege access.

**question_clarity**

The question is clear and directly asks about PCI-DSS requirements for Kubernetes clusters handling cardholder data.

**distractor_quality**

Distractors are plausible to novices but clearly wrong: A contradicts basic security, C is a common misconception, and D is a fabricated but plausible-sounding claim about PCI SSC approval.

**explanation_accuracy**

The explanation accurately maps controls to specific PCI-DSS v4.0 requirements (Req 1, 4, 6, 7, 8, 10, 11) and is technically sound.

**difficulty_calibration**

Medium is appropriate as it requires familiarity with both PCI-DSS and Kubernetes security controls.

**domain_relevance**

Directly tests Compliance and Security Frameworks knowledge, specifically PCI-DSS applied to Kubernetes.

### kcsa-092

**Per-dimension analysis:**

**answer_correctness**

Answer D is correct. Admission controllers are enabled via kube-apiserver flags (--enable-admission-plugins/--disable-admission-plugins), and inspecting the static pod manifest or pod spec is the standard way to verify them. The other options reference non-existent resources or wrong commands.

**question_clarity**

The question is clear: it asks how to verify enabled admission controllers on a running kube-apiserver. The wording is unambiguous.

**distractor_quality**

Distractors are plausible to novices: A invents a kubectl resource, B uses a plausible-sounding describe command, C confuses version checks with config inspection. All clearly wrong to an expert.

**explanation_accuracy**

The explanation correctly identifies the flags (--enable-admission-plugins, --disable-admission-plugins) and notes there is no API resource listing active admission controllers. Accurate.

**difficulty_calibration**

Medium is appropriate — requires knowledge of kube-apiserver static pod layout and admission controller configuration mechanism, beyond entry-level but not advanced.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, as admission controllers are a core security control point in the API server.

### kcsa-055

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Setting readOnlyRootFilesystem: true causes the container's root filesystem to be mounted read-only, with writes only possible to explicitly mounted writable volumes. This aligns with Kubernetes SecurityContext documentation.

**question_clarity**

The question is clear and unambiguous, asking specifically what the setting accomplishes.

**distractor_quality**

Distractors are plausible misconceptions: A confuses image layers vs runtime filesystem, C and D are plausible scope confusions about what 'read-only' applies to. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation is accurate, covering the security benefit (limiting attacker persistence) and the correct workaround (emptyDir/PVC volumes for writable paths).

**difficulty_calibration**

Medium is appropriate—this is a common CKS/KCSA hardening concept that requires understanding container filesystems, not just trivial recall.

**domain_relevance**

Directly tests Platform Security / container hardening via SecurityContext, fitting the stated domain.

### kcsa-029

**Per-dimension analysis:**

**answer_correctness**

Option A is correct. The primary and well-documented defense against pod-to-IMDS SSRF is blocking egress to 169.254.169.254 via NetworkPolicy (or CNI-level rules). Option B reduces ServiceAccount token theft but doesn't stop IMDS access; C and D don't address metadata endpoint access.

**question_clarity**

The question is clear and specific: it asks for the primary defense against SSRF targeting the instance metadata service from a compromised pod. No ambiguity.

**distractor_quality**

Distractors are plausible security-related controls (token automount, PodSecurity, CNI choice) but none directly block metadata endpoint egress. They appeal to learners who conflate general pod hardening with metadata protection.

**explanation_accuracy**

Explanation is accurate: it identifies the metadata IP, the credential-theft risk, the egress NetworkPolicy mitigation, and correctly notes IMDSv2 as an additional AWS-specific mitigation.

**difficulty_calibration**

Hard is reasonable — requires knowledge of cloud metadata service IP, SSRF mechanics, and Kubernetes egress controls. Could arguably be medium, but hard is acceptable.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain (cloud-native attack surface, credential exposure via metadata service).

### kcsa-152

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly identifies GDPR's core technical and organizational requirements (Articles 25 and 32): data minimization, encryption at rest/transit, access controls, audit logging, and data subject rights including erasure (Article 17). The other options are factually wrong: GDPR applies to all processing infrastructure including containers (not just databases), does not mandate EU physical hosting (transfers are allowed with safeguards like SCCs), and does not mandate EU-certified algorithms.

**question_clarity**

The question is clear and direct, asking what GDPR requires for Kubernetes clusters processing EU personal data. No ambiguity.

**distractor_quality**

Distractors are plausible misconceptions held by practitioners new to GDPR (data residency myth, scope misconception, encryption certification myth) but clearly wrong to experts. Good quality distractors.

**explanation_accuracy**

Explanation accurately cites GDPR Articles 25 (privacy by design) and 32 (security of processing), and correctly maps requirements to Kubernetes constructs (EncryptionConfiguration, mTLS, RBAC, audit logs, NetworkPolicy). The note about erasure extending to logs and backups is accurate and pedagogically valuable.

**difficulty_calibration**

Hard is appropriate as it requires understanding both GDPR legal requirements and their Kubernetes-specific technical implementations, plus distinguishing common myths from actual requirements.

**domain_relevance**

Directly tests Compliance and Security Frameworks domain (GDPR compliance for Kubernetes).

### kcsa-075

**Per-dimension analysis:**

**answer_correctness**

Rekor is indeed Sigstore's transparency log component, providing an immutable append-only ledger for signatures and attestations. Answer C is correct per official Sigstore documentation.

**question_clarity**

The question is clear and directly asks which Sigstore component serves as the transparency log. No ambiguity.

**distractor_quality**

All distractors are real Sigstore components (Cosign for signing, Fulcio for keyless CA, Gitsign for git commit signing), making them plausible to someone unfamiliar with the project's architecture.

**explanation_accuracy**

The explanation accurately describes Rekor as an immutable append-only transparency log and correctly identifies the roles of Cosign, Fulcio, and Gitsign.

**difficulty_calibration**

Labeled 'hard' but this is really a medium-level recall question about Sigstore component names. However, given that Sigstore internals are niche knowledge for KCSA candidates, 'hard' is defensible and not a clear mismatch.

**domain_relevance**

Supply chain security and Sigstore fall within Cloud Native Security overview, making this domain-appropriate.

### kcsa-088

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Kubernetes' OIDC authentication integration allows users to authenticate to the API server using JWT ID tokens issued by external identity providers like Dex, Okta, or Keycloak, configured via --oidc-issuer-url and --oidc-client-id flags.

**question_clarity**

The question is clear and asks two related things (what OIDC integration is and what it enables), which option B addresses directly. Phrasing is acceptable.

**distractor_quality**

Distractors are plausible: A confuses OIDC with workload identity/IRSA-style flows; C is a real desire but not what OIDC integration does (RBAC bindings must be manually created against OIDC groups); D confuses OIDC with TLS auth. All are wrong but plausible to a learner.

**explanation_accuracy**

Explanation accurately describes flags, JWT bearer token flow, claim mapping (sub/email/groups) to username/groups for RBAC, and benefits like SSO/MFA. Technically correct per Kubernetes docs.

**difficulty_calibration**

Medium is appropriate — requires understanding of authentication flows beyond basics but not deep CKS-level configuration.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically API server authentication mechanisms.

### kcsa-077

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The Kubernetes CertificateSigningRequest API is precisely designed to provide a Kubernetes-native workflow for requesting and approving X.509 certificates, commonly used for kubelet bootstrapping and serving certs, signed by configured signers including the cluster CA.

**question_clarity**

The question is clear and directly asks about the purpose of the CSR API. No ambiguity in phrasing.

**distractor_quality**

Distractors are plausible misconceptions: A conflates ingress TLS, C conflates etcd encryption, D conflates image signing/verification. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

Explanation correctly describes the CSR API workflow, mentions node bootstrapping and user cert creation, and notes the security risk of auto-approval, which is relevant for KCSA.

**difficulty_calibration**

Medium is appropriate — recognizing the CSR API's purpose and security implications requires more than basic knowledge.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security knowledge regarding PKI and the CSR API.

### kcsa-057

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Kubernetes audit logging records API server request activity and is configured via --audit-policy-file on the kube-apiserver, matching official documentation.

**question_clarity**

The question is clear, asking both what audit logging records and how it's configured. Option B addresses both parts directly.

**distractor_quality**

Distractors are plausible — they reference real Kubernetes concepts (container logging DaemonSets, kubelet config, imagePullPolicy) that a less knowledgeable test-taker might confuse with audit logging.

**explanation_accuracy**

The explanation accurately describes audit policy levels (None, Metadata, Request, RequestResponse), the flags (--audit-log-path, --audit-webhook-config-file), and relevance to compliance frameworks. All factually correct.

**difficulty_calibration**

Medium is appropriate — requires knowledge of both the concept and configuration mechanism, beyond basic recall but not deeply scenario-based.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security; audit logging on the API server is a core security control.

### kcsa-111

**Per-dimension analysis:**

**answer_correctness**

B accurately defines lateral movement in a security context: an attacker leveraging access from one compromised resource (the pod) to reach other resources within the cluster. This aligns with MITRE ATT&CK and Kubernetes threat modeling literature.

**question_clarity**

The question is clear and specifies the context (compromised Kubernetes pod), so the security meaning is unambiguous.

**distractor_quality**

A (pod rescheduling), C (kubelet moving containers between namespaces — which doesn't happen), and D (rolling updates) are all plausible-sounding to novices who might confuse 'movement' with operational mechanics, but clearly wrong to anyone familiar with security terminology.

**explanation_accuracy**

The explanation correctly describes lateral movement vectors (SA token abuse, pod network pivoting, cloud metadata IAM theft) and lists appropriate mitigations (RBAC, NetworkPolicy, disabling automount, runtime monitoring with Falco). All technically accurate.

**difficulty_calibration**

Medium is appropriate — requires knowing security terminology beyond basic Kubernetes operations but isn't a deep multi-step scenario.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain of KCSA, covering attacker techniques post-compromise.

### kcsa-083

**Per-dimension analysis:**

**answer_correctness**

--client-cert-auth=true is the correct etcd flag that enforces certificate-based client authentication. This matches official etcd documentation and CIS Kubernetes Benchmark control 2.2.

**question_clarity**

The question clearly asks for the specific etcd flag enforcing client certificate authentication. No ambiguity.

**distractor_quality**

--auto-tls is a real etcd flag (auto-generates TLS certs, plausible distractor). --peer-auth-required and --require-client-ca are fabricated but plausible-sounding to someone unfamiliar with etcd. Distractors are reasonable.

**explanation_accuracy**

Explanation correctly describes the flag's behavior, references --trusted-ca-file and the analogous --peer-client-cert-auth, and mentions CIS Benchmark requirement. All accurate.

**difficulty_calibration**

Hard is appropriate given this requires recall of specific etcd flag names and CIS Benchmark knowledge, which is detailed cluster hardening content.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security (etcd hardening), squarely in the stated domain.

### kcsa-119

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. kubectl port-forward establishes a streaming tunnel through the API server to the kubelet, which then connects to the specified pod port. This traffic does not traverse Services or Ingress, and because it terminates at the pod from the node-local kubelet, it typically isn't filtered by NetworkPolicy ingress rules either. This is well-documented behavior.

**question_clarity**

The question is clear and specific, asking about the security risk of port-forward access. The phrasing is unambiguous and directly indicates what's being assessed.

**distractor_quality**

Distractors are reasonably plausible. A (NetworkPolicy bypass via CNI inspection) is a tempting half-truth. C (filesystem exposure) confuses port-forward with exec/cp. D (kubelet logs traffic) is a plausible-sounding but false claim. None are correct, and B is clearly the most complete answer.

**explanation_accuracy**

The explanation accurately describes the tunnel mechanism, the RBAC permission (pods/portforward subresource), and the practical security implication. The guidance to treat pods/portforward as equivalent to unrestricted pod network access is sound and aligns with CNCF/security best practices.

**difficulty_calibration**

Medium is appropriate. This requires understanding of API server proxying, RBAC subresources, and NetworkPolicy scope — beyond basic knowledge but not deeply advanced.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, addressing attack surface via API server access paths and privilege escalation via subresource RBAC permissions.

### kcsa-145

**Per-dimension analysis:**

**answer_correctness**

Option A is correct. As of Kubernetes 1.30, AppArmor graduated to GA and is configured via the appArmorProfile field in securityContext (pod or container level), with type values RuntimeDefault, Localhost, or Unconfined. This replaces the prior annotation-based approach.

**question_clarity**

The question is clear: it asks what AppArmor is in Kubernetes and how it's configured post-GA in 1.30. The compound nature is slightly awkward but the options focus on configuration mechanism, which is testable.

**distractor_quality**

Distractors are plausible: B (DaemonSet for profile loading) reflects a real pattern operators use to load profiles onto nodes; C (ConfigMap) is a believable misconception; D (kubelet flag) is plausible-sounding but incorrect. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes the appArmorProfile field, lists the three valid types with correct semantics, notes the prior annotation method, and correctly characterizes AppArmor's scope (file/capability/network restrictions). Factually sound.

**difficulty_calibration**

Hard is reasonable given this requires specific knowledge of the 1.30 GA API change from annotations to the structured field. Medium would also be defensible, but hard is acceptable.

**domain_relevance**

Directly tests Platform Security knowledge—AppArmor is a core Linux MAC mechanism integrated into Kubernetes pod security.

## Fixed Inline (llm_validated=1)

### kcsa-107

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in option B and in the explanation where em dashes should be.
- **WARNING** `question_clarity` — The question stem asks what namespace isolation does NOT protect against, but the correct option B is framed as a capability description rather than a threat. Rewording the stem to 'Which statement correctly describes a limitation of Kubernetes namespace isolation?' would align better with the answer options.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and explanation.
- Reworded the question stem to align with how option B is phrased (statement-of-fact about a limitation).

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct: Kubernetes namespaces do not provide network isolation by default — pods across namespaces can communicate freely on the pod network unless a NetworkPolicy is applied. This is well-documented in the official Kubernetes networking and namespace documentation. Options A, C, and D are all factually incorrect statements about namespaces.

**question_clarity**

The stem 'What does namespace isolation NOT protect against?' is slightly awkward because the correct option B is phrased as a descriptive statement about namespace capabilities rather than as a threat/risk that isn't prevented. A learner must interpret this as 'which statement correctly identifies a limitation' — the meaning is recoverable but the framing is loose. Still, the intent is clear enough.

**distractor_quality**

Distractors A, C, and D represent common misconceptions (that namespaces enforce network isolation, restrict ClusterRole access, or equate to cluster-level isolation). They are plausible to beginners and clearly wrong to experts. Quality is good.

**explanation_accuracy**

The explanation is technically accurate: namespaces scope RBAC, ResourceQuotas, and LimitRanges but not network traffic; NetworkPolicy is required for network segmentation; and namespaces are not a security boundary against untrusted workloads. The 'separate namespace' misconception note is a useful and accurate clarification.

**difficulty_calibration**

Medium is appropriate — this tests a frequently misunderstood security concept requiring knowledge of namespaces, RBAC scope, and NetworkPolicy interaction. Not trivial for beginners, but not deeply scenario-based either.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals: the limits of namespace as a security boundary, NetworkPolicy requirements, and isolation guarantees are core KCSA topics.

### kcsa-089

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in options A, B, C, D instead of an em dash, which looks unprofessional and may confuse readers.
- **WARNING** `explanation_accuracy` — Same mojibake 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with em dash '—' in all four options and in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Kubernetes user impersonation is performed via Impersonate-User, Impersonate-Group, Impersonate-Uid, and Impersonate-Extra HTTP headers, and the RBAC verb 'impersonate' on the 'users', 'groups', or 'serviceaccounts' resources is required. This matches official Kubernetes authentication documentation.

**question_clarity**

The question clearly asks two things: what impersonation is and which RBAC verb controls it. The phrasing is unambiguous.

**distractor_quality**

Distractors are plausible-sounding to novices (referencing real Kubernetes concepts like PodSecurity, NodeRestriction, leader election) but clearly wrong to experts since none of those mechanisms involve user impersonation.

**explanation_accuracy**

The explanation accurately describes the headers, the verb, kubectl --as/--as-group usage, and correctly warns about the privilege-escalation risk. Technically Impersonate-Uid header also exists but its omission is not a flaw.

**difficulty_calibration**

Labeled hard, which is slightly aggressive — this is more of a medium-level concept — but acceptable since impersonation is an advanced security topic many practitioners don't know. Not a clear mismatch.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security as it concerns API server authentication and RBAC.-

### kcsa-117

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in option A instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with an em dash '—' in option A.

**Per-dimension analysis:**

**answer_correctness**

Option A is definitively correct. Direct etcd access bypasses the kube-apiserver entirely, which means it bypasses RBAC, admission controllers, and audit logging, granting full read/write access to all cluster state including Secrets. This is equivalent to cluster-admin (and arguably worse, since it leaves no audit trail).

**question_clarity**

The question clearly asks about the etcd direct access attack technique and why it equates to cluster-admin privileges. The phrasing is unambiguous, though option A contains a mojibake artifact (â€”) instead of an em dash.

**distractor_quality**

Distractors B, C, and D are plausible misconceptions for less-experienced practitioners: B suggests only performance impact, C falsely claims Secrets are stored separately, and D fabricates an alerting mechanism. All are clearly wrong to an expert while being instructive.

**explanation_accuracy**

The explanation is accurate: etcd does store Kubernetes state unencrypted by default unless EncryptionConfiguration at rest is configured, direct etcdctl access bypasses RBAC/audit, and mTLS plus network restriction are the proper mitigations. Content is technically sound.

**difficulty_calibration**

Hard is reasonable given that it requires understanding of the control plane architecture, etcd's role, encryption-at-rest defaults, and the audit/RBAC bypass implications. A KCSA candidate would need solid threat-model knowledge to answer confidently.

**domain_relevance**

This directly fits the Kubernetes Threat Model domain — etcd compromise is a canonical persistence/privilege-escalation attack vector covered in KCSA threat modeling content.

### kcsa-116

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifacts (â€") appear twice in the explanation in place of em dashes.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in the explanation (two occurrences).

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Taints and tolerations combined with dedicated node pools is the canonical Kubernetes mechanism for ensuring sensitive/trusted workloads run on isolated nodes, away from untrusted ones. PodAntiAffinity (A) could express 'don't co-locate' but is brittle at scale, requires labeling every untrusted pod consistently, and is commonly noted as not sufficient or practical for trust-boundary isolation. NetworkPolicy (C) governs traffic, not scheduling. LimitRange (D) is about resource quotas.

**question_clarity**

The question is clear and unambiguous, asking specifically about a scheduling-level isolation feature.

**distractor_quality**

Distractors are plausible: A is a real scheduling constraint, C and D are real Kubernetes objects that a learner might confuse with isolation primitives. All are clearly weaker than B for an expert.

**explanation_accuracy**

The explanation correctly describes taints as a repellent mechanism and notes the need to combine with NodeAffinity/RBAC to prevent toleration abuse — this is accurate guidance. However, the explanation contains mojibake (â€") instead of em dashes.

**difficulty_calibration**

Medium is appropriate — requires understanding of multiple scheduling primitives and their security implications.

**domain_relevance**

Directly tests Kubernetes Threat Model concepts around workload isolation and trust boundaries.

### kcsa-066

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears instead of an em dash in the explanation.

**Fixes applied:**

- Fixed mojibake encoding artifact â€” → — in explanation.

**Per-dimension analysis:**

**answer_correctness**

Namespace (C) is the correct primary unit of tenant separation in Kubernetes multi-tenancy. Official Kubernetes multi-tenancy documentation explicitly identifies namespaces as the boundary for RBAC, quotas, and network policies. Containers, Pods, and Nodes are not designed as tenant boundaries.

**question_clarity**

The question is clear and direct, asking for the primary isolation boundary for tenant separation in a multi-tenant cluster.

**distractor_quality**

Container, Node, and Pod are plausible distractors — Node especially could tempt those thinking about hard isolation, while Pod and Container relate to workload boundaries. All are clearly incorrect to an expert.

**explanation_accuracy**

The explanation is factually accurate: namespaces scope RBAC, quotas, and policies; isolation is soft due to shared kernel; gVisor/Kata and node pools provide stronger isolation. Only issue is a mojibake artifact 'â€”' instead of an em dash.

**difficulty_calibration**

Medium is appropriate — requires understanding of Kubernetes multi-tenancy concepts beyond pure definitions.

**domain_relevance**

Directly relevant to Cloud Native Security overview, covering tenant isolation fundamentals.

### kcsa-140

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in options A, B, and D instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in options A, B, and D.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Volume-mounted secrets are updated by kubelet within the sync period (kubelet syncFrequency + cache TTL), while environment variable secrets are immutable for the pod lifecycle and require restart. This aligns with official Kubernetes documentation on Secret consumption.

**question_clarity**

The question is clear, asking for both definition and the key challenge. The two-part structure is well-matched by option B which addresses both aspects.

**distractor_quality**

Distractors are plausible: A confuses Secret rotation with CA cert rotation, C confuses with etcd encryption key rotation (a real but different concept), D conflates with API server TLS rotation. All are real Kubernetes concepts that could mislead a learner.

**explanation_accuracy**

Explanation is accurate: kubelet refresh period is approximately 1 minute by default (syncFrequency 1min + cache TTL), env var secrets indeed require pod restart, and the note about applications needing to handle the new value is an important real-world consideration.

**difficulty_calibration**

Medium is appropriate — requires understanding of Secret consumption modes and kubelet behavior, beyond beginner level but not deeply advanced.

**domain_relevance**

Directly relevant to Platform Security domain, covering Secrets management which is a core platform security concern.

### kcsa-148

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with an em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

FedRAMP is correct — it is the US federal program for authorizing cloud services and references NIST 800-53 and 800-190 which include container/Kubernetes guidance. SOC 2, PCI-DSS, and ISO 27001 are not US-federal-specific frameworks.

**question_clarity**

The question clearly asks for a US federal cloud compliance framework with Kubernetes guidance. Phrasing is unambiguous.

**distractor_quality**

SOC 2, PCI-DSS, and ISO 27001 are all well-known compliance frameworks that serve as plausible but clearly incorrect distractors since none are specifically US federal cloud programs.

**explanation_accuracy**

Content is accurate: FedRAMP uses NIST 800-53 baselines, references NIST 800-190 for containers, and SSDF/800-218 applies to supply chain. However, the explanation contains a mojibake artifact 'â€”' instead of an em dash.

**difficulty_calibration**

Medium is appropriate — requires knowing multiple frameworks and their jurisdictional scope.

**domain_relevance**

Directly tests knowledge of compliance frameworks relevant to Kubernetes, matching the stated domain.

### kcsa-135

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear instead of em dashes in options A, C and throughout the explanation.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in options A and C and in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. Istio AuthorizationPolicy operates at L7 (HTTP/gRPC) and is enforced by Envoy sidecars, while Kubernetes NetworkPolicy operates at L3/L4. They are complementary, not replacements.

**question_clarity**

The question is clear and asks for both a definition and a comparison, which option A succinctly addresses.

**distractor_quality**

Distractors are plausible: B confuses enforcement (kube-proxy vs Envoy), C suggests a branding equivalence, D inverts the scope. All are clearly wrong to experts but tempting to novices.

**explanation_accuracy**

The explanation is accurate regarding the L3/L4 vs L7 distinction and example capabilities. However, mojibake encoding artifacts (â€") appear in place of em dashes in both options and explanation.

**difficulty_calibration**

Medium is appropriate — it requires understanding two different policy systems and their layer of operation.

**domain_relevance**

Directly relevant to Platform Security, covering service mesh authorization vs network-layer policies.

### kcsa-037

**Issues found:**

- **WARNING** `difficulty_calibration` — The question is a definitional/recall question with clearly distinguishable distractors; 'hard' overstates the difficulty. 'Medium' is more appropriate.

**Fixes applied:**

- Lowered difficulty from 'hard' to 'medium' to reflect the definitional nature of the question.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Admission webhooks are HTTP callbacks invoked by the API server, and the two types are MutatingAdmissionWebhook and ValidatingAdmissionWebhook, per official Kubernetes documentation.

**question_clarity**

The question is clear and asks two related things: definition and types. Phrasing is unambiguous.

**distractor_quality**

Distractors are plausible-sounding fabrications (Startup/Shutdown, Informer/Lister, Ingress/Egress) that an unfamiliar learner might consider but an expert recognizes as wrong. Quality is good.

**explanation_accuracy**

The explanation is accurate: mutating runs before validating, mutating can modify objects, validating cannot, and failurePolicy governs behavior on timeout/failure. All consistent with Kubernetes docs.

**difficulty_calibration**

Labeled 'hard' but this is essentially a definitional question about a well-known concept. 'Medium' would be more appropriate, though this is a minor calibration concern.

**domain_relevance**

Admission webhooks are central to Platform Security (policy enforcement, OPA/Gatekeeper, Kyverno), so domain relevance is solid.

### kcsa-130

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in option B instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Option B correctly defines container escape and identifies privileged:true / dangerous capabilities like SYS_ADMIN as the most common precondition. This aligns with Kubernetes security documentation and CNCF threat modeling guidance. Other options describe normal lifecycle events or unrelated supply chain issues.

**question_clarity**

The question clearly asks for both the definition and the most common precondition. The stem is unambiguous and well-scoped.

**distractor_quality**

Distractors A, C, D are plausible Kubernetes events (pod completion, image pull policy, eviction) that someone unfamiliar might confuse with security incidents, but each is clearly not a container escape to an expert.

**explanation_accuracy**

The explanation accurately lists common escape vectors (privileged mode, dangerous capabilities, hostPath mounts, kernel CVEs) and reasonable mitigations. Technically accurate.

**difficulty_calibration**

Hard is slightly aggressive — this is more medium since the correct answer is identifiable by recognizing the definition. However, the nuance around capabilities justifies hard as acceptable.

**domain_relevance**

Directly relevant to Kubernetes Threat Model domain, covering container escape attack vectors.

### kcsa-121

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in both option B and the explanation where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Falco uses syscall monitoring via eBPF or a kernel module, and detects shell spawning by matching execve calls against shell binary names in container contexts. The other options describe mechanisms Falco does not use for this detection.

**question_clarity**

The question is clear and asks specifically about Falco's detection mechanism for shell spawning at runtime.

**distractor_quality**

Distractors are plausible: audit logs (A) is a real detection avenue but for a different tool/scope, filesystem changes (C) sounds like FIM tools, and DNS monitoring (D) is plausible for other threats. All are clearly wrong to someone familiar with Falco.

**explanation_accuracy**

The explanation accurately describes Falco's architecture (eBPF/kernel module intercepting syscalls like execve) and the rule matching against shell binaries. Contains mojibake encoding artifacts (â€") instead of em dashes.

**difficulty_calibration**

Hard is reasonable given the specificity about eBPF/kernel modules and execve syscall semantics, though it could arguably be medium. Acceptable as-is.

**domain_relevance**

Directly relevant to Kubernetes Threat Model domain — runtime threat detection is a core topic.

### kcsa-093

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The TokenRequest API (projected service account tokens) issues time-bound, audience-scoped tokens that can be bound to a pod's lifetime, replacing the legacy auto-generated non-expiring Secret tokens. This matches official Kubernetes documentation.

**question_clarity**

The question is clear and asks both what TokenRequest is and how it improves on legacy tokens. Option B addresses both aspects directly.

**distractor_quality**

Distractors are plausible: A confuses authentication with authorization, C references the real but distinct bootstrap token mechanism, and D describes a related but incorrect use case. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation is factually accurate regarding time-limited, audience-bound, and pod-bound properties, and correctly notes legacy Secret tokens did not expire. However, there is a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Hard is reasonable given this requires knowledge of multiple token properties (audience binding, pod binding, expiry) and the historical context of legacy ServiceAccount Secrets.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, covering API server authentication mechanisms.

### kcsa-147

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in option B and the explanation instead of em dashes (—).

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and in explanation.

**Per-dimension analysis:**

**answer_correctness**

Option B correctly describes Pod Topology Spread Constraints: distributing pods across topology domains (nodes/zones) to reduce blast radius. This aligns with Kubernetes documentation. Other options are factually incorrect descriptions.

**question_clarity**

The question is clear and asks for both the definition and security relevance, which option B addresses.

**distractor_quality**

Distractors are plausible: A confuses it with NetworkPolicy/zone affinity, C invents a namespace-spreading concept, D references image layer distribution. All are clearly wrong to experts but plausible to novices.

**explanation_accuracy**

The explanation is factually accurate regarding the blast radius reduction and availability benefits. However, it contains mojibake encoding artifacts (â€") that should be em dashes.

**difficulty_calibration**

Medium is appropriate — requires knowledge of a specific scheduling feature plus security reasoning about blast radius.

**domain_relevance**

Tests Platform Security concepts (blast radius reduction, availability/CIA triad via Kubernetes scheduling primitive), which fits the stated domain.

### kcsa-137

**Issues found:**

- **WARNING** `question_clarity` — Option C contains a mojibake encoding artifact 'â€"' instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option C.

**Per-dimension analysis:**

**answer_correctness**

B correctly describes the baseline profile, which prevents known privilege escalations while permitting most apps to run unmodified. This aligns with the official PSS documentation. A describes restricted (non-root enforcement), C is fabricated, D describes privileged.

**question_clarity**

The question clearly asks about the purpose of the baseline PSS profile. Unambiguous.

**distractor_quality**

Distractors are plausible: A confuses baseline with restricted, D with privileged, C invents a purpose. All are clearly wrong to experts but tempting to learners.

**explanation_accuracy**

Accurate description of baseline restrictions and contrasts with restricted/privileged. Contains a mojibake artifact (â€") in option C but the explanation content itself is correct.

**difficulty_calibration**

Medium is appropriate — requires knowing the three PSS tiers and their distinctions.

**domain_relevance**

Directly tests Platform Security / PodSecurity Standards knowledge.

### kcsa-036

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€"' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€"' with an em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. imagePullPolicy: Always causes the kubelet to consult the registry and compare digests on each pod start, which mitigates risk from stale cached images. The other options are factually incorrect descriptions of the setting.

**question_clarity**

The question clearly asks about the purpose of imagePullPolicy: Always and its security relevance. Phrasing is unambiguous.

**distractor_quality**

Distractors are plausible misconceptions: A inverts the actual behavior, C confuses pull policy with imagePullSecrets, and D conflates pull with build. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation is accurate: Always triggers registry check, mutable tags are a risk, and digest pinning is the strongest guarantee. Contains a mojibake artifact 'â€"' that should be an em dash.

**difficulty_calibration**

Medium is appropriate — it requires understanding both the mechanical behavior and the security implication around mutable tags.

**domain_relevance**

Directly relevant to Platform Security (supply chain / image security).

### kcsa-084

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts 'â€"' appear in options A and B instead of em dashes (—).

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in options A and B.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The Kubernetes Aggregated API server pattern does register extension API servers via APIService objects, and the main kube-apiserver proxies requests to them after authentication, passing identity via X-Remote-User/Group headers. A compromised aggregated API server can indeed see all proxied traffic, making this a legitimate security consideration.

**question_clarity**

The question clearly asks about a security consideration of the Aggregated API Server pattern. It is specific and unambiguous.

**distractor_quality**

Distractors are plausible misconceptions: A confuses aggregation of API servers with multi-cluster aggregation; C confuses with audit log aggregation; D confuses with load balancing. All are clearly wrong to someone who knows the pattern but plausible to novices.

**explanation_accuracy**

The explanation accurately describes the APIService registration, the use of X-Remote-User/Group headers, the trust model, and the importance of requestheader-client-ca-file validation. All technically correct.

**difficulty_calibration**

Hard is appropriate — this requires understanding of extension API server architecture, request proxying, and header-based authentication trust, which is advanced material.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security regarding API server extension architecture.**.**

### kcsa-124

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear in all four option strings instead of em dashes (—).

**Fixes applied:**

- Replaced mojibake â€" with em dash — in all four options.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The noisy neighbor problem in multi-tenant Kubernetes refers to one tenant's workload consuming disproportionate resources and degrading performance for others. ResourceQuota and LimitRange are the canonical Kubernetes-native mitigations documented in official docs.

**question_clarity**

The question is clear and directly asks for both the definition and the mitigating resources. The phrasing is unambiguous.

**distractor_quality**

Distractors are plausible: A references real log volume concerns, C references network behavior (though NetworkPolicy does not actually do bandwidth limiting — making it a good distractor for those who don't know NetworkPolicy's actual function), D references a concurrency scenario. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

The explanation accurately describes the noisy neighbor problem, the role of ResourceQuota (namespace-level aggregate limits) and LimitRange (per-container defaults/bounds), and the consequences (OOMKills, CPU starvation). Technically sound.

**difficulty_calibration**

Medium is appropriate — requires understanding multi-tenancy concepts plus two related but distinct Kubernetes resources.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, specifically multi-tenancy isolation threats.

### kcsa-048

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in option B instead of an em dash '—'.

**Fixes applied:**

- Replaced mojibake 'â€”' with em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. A wildcard in verbs or resources fields in RBAC rules grants all verbs or all resource types, which is widely recognized as a least-privilege anti-pattern per CIS Kubernetes Benchmark. The other options are factually wrong: wildcards don't affect namespace scope (A), don't change Role to ClusterRole (C), and RBAC has no built-in MFA mechanism (D).

**question_clarity**

The question is clear and unambiguous, directly asking about the semantic meaning of a wildcard in RBAC verb/resource fields. No double negatives or vague phrasing.

**distractor_quality**

Distractors are reasonably plausible for a beginner: A confuses scope concepts, C confuses Role vs ClusterRole, and D introduces an unrelated security feature. All are clearly wrong to an expert while tempting for novices.

**explanation_accuracy**

The explanation is accurate — wildcards grant all actions/resources, this violates least privilege, and CIS Benchmarks do flag this. The recommendation to enumerate specific verbs is sound guidance.

**difficulty_calibration**

Easy is appropriate; this tests a single, well-known concept about wildcard semantics in RBAC.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (RBAC least privilege), which is core KCSA content.

### kcsa-046

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifacts â€" appear instead of em dashes in option A and in the explanation.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option A and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer C is correct per Kubernetes NetworkPolicy spec: an empty podSelector ({}) selects all pods in the namespace. This is the documented default-deny pattern.

**question_clarity**

The question is clear and unambiguous, asking specifically about an empty podSelector ({}) within a namespace scope.

**distractor_quality**

Distractors are plausible: A reflects a common misconception that empty selectors match nothing (vs LabelSelector semantics), B confuses empty selector with selecting unlabeled pods, D tests namespace scope understanding. All are reasonable misconceptions.

**explanation_accuracy**

The explanation is factually accurate regarding the default-deny pattern with empty podSelector and empty policyTypes rules. However, mojibake artifacts (â€") appear instead of em dashes in both options and explanation.

**difficulty_calibration**

Medium is appropriate — this requires understanding LabelSelector semantics and the default-deny pattern, beyond basic recall but not deeply complex.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals via NetworkPolicy semantics, a core KCSA topic.

### kcsa-112

**Issues found:**

- **WARNING** `question_clarity` — Option B contains a mojibake encoding artifact 'â€"' instead of an em dash (—).

**Fixes applied:**

- Replaced mojibake 'â€"' with an em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The ServiceAccount token mounted at /var/run/secrets/kubernetes.io/serviceaccount/token is the standard pivot point for attackers to query the Kubernetes API after pod compromise. The other options describe unrelated attack techniques (container escape, DNS, image tampering).

**question_clarity**

The question is clear and specifically asks about the technique that uses a mounted ServiceAccount token for API enumeration and privilege escalation.

**distractor_quality**

All three distractors are plausible Kubernetes attack techniques (privileged container escape, DNS attacks, supply chain image tampering) but none involve the ServiceAccount token mechanism, making them clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes the technique, including kubectl auth can-i --list enumeration and standard mitigations (automountServiceAccountToken: false, RBAC least privilege, NetworkPolicy).

**difficulty_calibration**

Medium is appropriate — requires knowing both the token location and the attack pattern, beyond basic recall.

**domain_relevance**

Directly tests Kubernetes Threat Model knowledge, specifically credential abuse and privilege escalation paths.

### kcsa-159

**Issues found:**

- **WARNING** `question_clarity` — Option B contains a mojibake encoding artifact 'â€"' that should be an em dash '—'.

**Fixes applied:**

- Fixed mojibake encoding artifact â€" → — in option B.

**Per-dimension analysis:**

**answer_correctness**

Option B correctly describes D3FEND as a knowledge graph of defensive countermeasures complementary to ATT&CK, which aligns with MITRE's official description. The other options are factually incorrect (D3FEND is not deprecated, not a NIST framework, not a CVE scoring system).

**question_clarity**

The question is clear and asks two related things: what D3FEND is and how it complements ATT&CK. Option B addresses both aspects.

**distractor_quality**

Distractors are plausible to novices but clearly wrong to experts: A confuses deprecation, C misattributes to NIST and narrows to eBPF, D confuses with CVSS-like scoring. All are reasonable misconceptions.

**explanation_accuracy**

The explanation accurately characterizes D3FEND's defensive technique categories (Harden, Detect, Isolate, Evict are real D3FEND tactics) and its relationship with ATT&CK. Technically sound.

**difficulty_calibration**

Hard is appropriate as D3FEND is a less commonly known framework compared to ATT&CK, and mapping defenses to attacks is an advanced security concept.

**domain_relevance**

Directly tests Compliance and Security Frameworks knowledge, specifically MITRE frameworks applied to Kubernetes.

### kcsa-078

**Issues found:**

- **ERROR** `answer_correctness` — Option A states profiling data is accessible via 'unauthenticated access', which is incorrect. The /debug/pprof endpoints on kube-apiserver are served through the standard authn/authz stack and require authenticated access. The explanation itself contradicts the option by noting endpoints are 'accessible to authenticated users'.
- **WARNING** `explanation_accuracy` — Explanation contradicts the chosen option regarding authentication, which would leave learners with a confused mental model about whether pprof is exposed without auth.

**Fixes applied:**

- Rewrote option A to remove the inaccurate 'unauthenticated' claim and accurately describe the risk as exposure of sensitive runtime/memory/goroutine information via pprof endpoints that could aid attackers who gain API access.
- Clarified explanation to align with corrected option and accurately describe authn/authz behavior of pprof endpoints.

**Per-dimension analysis:**

**answer_correctness**

Answer A is the intended best answer, but it contains a factual inaccuracy: it states profiling data is accessible via 'unauthenticated access'. In reality, the /debug/pprof endpoints on kube-apiserver are served behind the normal authentication/authorization stack — they are not unauthenticated. The explanation itself acknowledges this ('accessible to authenticated users'), directly contradicting the option text. The core security concern (exposing sensitive runtime information) is correct, but the 'unauthenticated' qualifier makes A technically incorrect as written.

**question_clarity**

The question stem is clear and well-scoped: it asks about the security risk of enabling --profiling on kube-apiserver in production. No ambiguity in what is being asked.

**distractor_quality**

Distractors are plausible and clearly wrong to an expert: B (TLS disablement) is fabricated behavior, C (etcd keyspace exposure) misattributes a different concern, D (JDWP) is a Java-specific protocol not applicable to Go. They are appropriately misleading without being trivially absurd.

**explanation_accuracy**

The explanation is mostly accurate — pprof endpoints are enabled by Go's net/http/pprof, CIS Benchmark does recommend --profiling=false (controls 1.2.14/1.2.19 etc.), and the endpoints reveal runtime internals. However, it contradicts the answer option by correctly stating the endpoints are accessible to 'authenticated users', whereas option A says 'unauthenticated access'.

**difficulty_calibration**

Medium is appropriate. Recognizing this CIS Benchmark hardening item requires more than entry-level knowledge but is a well-known production hardening recommendation.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security — specifically hardening of control plane component flags per CIS Benchmark, squarely within the KCSA domain.

### kcsa-024

**Issues found:**

- **WARNING** `difficulty_calibration` — The docker.sock mounting risk is a commonly taught container security topic and is better calibrated as 'medium' rather than 'hard' for KCSA scope.

**Fixes applied:**

- Adjusted difficulty from 'hard' to 'medium' to better match the conceptual level of this well-known container security topic.

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. Mounting /var/run/docker.sock grants the container full API access to the Docker daemon, which runs as root, enabling creation of privileged containers and host escape. This is well-documented in CIS benchmarks and container security literature.

**question_clarity**

The question is clear and direct, asking specifically about the security risk of mounting docker.sock into a container. No ambiguity in phrasing.

**distractor_quality**

Distractors A, C, and D are plausible to someone unfamiliar with Docker socket capabilities (they sound like reasonable limited-permission scenarios) but clearly wrong to experts who know the socket exposes the full Docker API.

**explanation_accuracy**

Explanation accurately describes the risk: root-equivalent Docker daemon access, ability to create privileged containers, host escape, and pivoting. The Kaniko recommendation is a valid alternative for in-cluster image builds.

**difficulty_calibration**

Labeled 'hard' but this is a well-known container security concept frequently covered in introductory container security material. More appropriately 'medium' given KCSA scope, though 'hard' is defensible since it requires understanding of Docker daemon privileges and escape techniques.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals, covering container isolation and host security risks.

### kcsa-150

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€"' appears in option B instead of an em dash or hyphen.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly defines SOC 2 Type II as an auditing standard (developed by AICPA) that evaluates control effectiveness over a period, and the Kubernetes controls listed (audit logging, RBAC, change management) are directly relevant to the Trust Service Criteria. Option A is wrong (CNCF doesn't issue SOC 2), C is wrong (SOC 2 is not specifically a 2FA requirement), and D mischaracterizes SOC 2 as a benchmark.

**question_clarity**

The question clearly asks two things: what SOC 2 Type II is, and which K8s controls map to its Trust Service Criteria. The compound question is acceptable since only one option addresses both parts correctly.

**distractor_quality**

Distractors are plausible to learners: A plays on confusion between SOC 2 and CNCF certifications, C confuses 2FA with SOC 2's security criteria, and D correctly attributes AICPA but mischaracterizes SOC 2 as a benchmark. All are clearly wrong to experts.

**explanation_accuracy**

The explanation accurately describes SOC 2 Type II's evaluation period and Trust Service Criteria. Note: the full TSC set is Security, Availability, Processing Integrity, Confidentiality, and Privacy; the explanation lists three but this is acceptable as the most commonly scoped. The mapped K8s controls are accurate.

**difficulty_calibration**

Medium is appropriate — requires knowledge of compliance frameworks and how K8s controls map to them, beyond pure K8s mechanics.

**domain_relevance**

Directly tests Compliance and Security Frameworks domain knowledge.

### kcsa-143

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears instead of an em dash in the explanation.
- **WARNING** `difficulty_calibration` — This is a single-concept definitional/recognition question about cert-manager. 'Hard' overstates the difficulty; 'medium' is more appropriate.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash in explanation.
- Lowered difficulty from 'hard' to 'medium' to match the definitional nature of the question.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly describes cert-manager as a Kubernetes operator/controller that automates X.509 certificate lifecycle management with multiple issuer types including Let's Encrypt (ACME), Vault, and self-signed CAs. This matches the official cert-manager documentation precisely.

**question_clarity**

The question is clear and asks a two-part definitional question about what cert-manager is and its role in TLS security. The phrasing is unambiguous.

**distractor_quality**

Distractors are plausible: A suggests a kubectl plugin (cert-manager is not a plugin), C confuses it with replacing the cluster CA (it doesn't replace kube-apiserver's CA), and D narrows it to mere validation. All are wrong but believable to a novice.

**explanation_accuracy**

The explanation accurately describes the certificate lifecycle, CRDs (Certificate, Issuer/ClusterIssuer), and use cases. Contains a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Labeling this as 'hard' is questionable since it is essentially a definitional/recognition question about a well-known CNCF project. 'Medium' or even 'easy' would be more appropriate, but this is a minor calibration issue.

**domain_relevance**

Directly relevant to Platform Security domain — cert-manager is a key tool for TLS/PKI management in Kubernetes.

### kcsa-139

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifacts (â€") appear in options A, B, C, D and the explanation instead of em dashes (—).

**Fixes applied:**

- Replaced mojibake â€" with em dash — in all options and the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. NetworkPolicy enforcement is delegated to the CNI plugin, and if the CNI doesn't support it, policies are silently ignored — this is documented Kubernetes behavior.

**question_clarity**

The question is clear and directly asks why CNI choice matters for NetworkPolicy enforcement.

**distractor_quality**

Distractors A, C, and D are plausible-sounding misconceptions about CNI responsibilities (CPU, TLS, image pulls) that someone unfamiliar might consider, but are clearly wrong to experts.

**explanation_accuracy**

The explanation is technically accurate regarding CNI support matrix (Flannel lacks NetworkPolicy, Calico/Cilium/Weave provide varying support). Contains mojibake encoding artifacts (â€") that should be em dashes.

**difficulty_calibration**

Medium is appropriate — requires understanding the delegation of NetworkPolicy enforcement to CNI plugins, not just basic definitions.

**domain_relevance**

Directly relevant to Platform Security, as NetworkPolicy enforcement is a core platform security concern.

### kcsa-041

**Issues found:**

- **WARNING** `explanation_accuracy` — Explanation reflects the pre-1.0 SLSA spec with four levels. SLSA v1.0 defines Build levels L1-L3 and organizes requirements into tracks; L4 was removed/reserved.
- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€"' appears instead of an em dash.

**Fixes applied:**

- Updated explanation to reflect SLSA v1.0 (Build track L1-L3) while noting the original four-level draft, and fixed the em-dash encoding artifact.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. SLSA is indeed a security framework that defines integrity levels for software build and supply chain processes, originally created by Google and now stewarded by OpenSSF. The other options are fabricated or incorrect.

**question_clarity**

The question is clear and unambiguous, asking for both the definition and purpose of SLSA. Two-part questions like this are acceptable since all parts align with the correct answer.

**distractor_quality**

Distractors are plausible to learners: A invokes Kubernetes storage terminology, C references a real signing tool (Cosign), and D ties to CI/CD compliance. All are clearly wrong to someone familiar with SLSA.

**explanation_accuracy**

The explanation describes the legacy SLSA draft (levels 1-4). SLSA v1.0 (released 2023) restructured the framework into Build levels L1-L3 (with L4 reserved/removed) and split requirements into tracks. Describing 'four integrity levels' with the old definitions is outdated and could mislead learners. Also contains a mojibake artifact 'â€"' instead of an em dash.

**difficulty_calibration**

Medium is appropriate — SLSA is a named framework that requires specific knowledge beyond entry-level but is widely covered in cloud-native security training.

**domain_relevance**

Directly relevant to 'Overview of Cloud Native Security' as supply chain security is a core topic.

### kcsa-058

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact â€" appears in option D and in the explanation instead of an em dash (—).

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option D and in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Option A correctly describes the distinction per the Kubernetes user-facing roles documentation: 'view' is read-only (get/list/watch on most namespaced resources, excluding secrets and roles/rolebindings), while 'edit' allows write access to most resources but excludes Role and RoleBinding modification to prevent privilege escalation. The other options are factually wrong.

**question_clarity**

The question is clear, direct, and asks an unambiguous comparison between two well-known default ClusterRoles.

**distractor_quality**

Distractors are plausible: B confuses ClusterRole binding scope with the role definition, C inverts a real concern about exec/logs (which 'edit' actually grants), and D is a tempting wrong answer for unfamiliar learners. All are clearly wrong to experts.

**explanation_accuracy**

The hierarchy described (view → edit → admin → cluster-admin) and the RBAC-escalation-prevention rationale are accurate. Minor omission: 'view' also excludes secrets by default, but this isn't required for the answer. Contains mojibake â€" instead of em dash.

**difficulty_calibration**

Easy is appropriate — this is foundational RBAC knowledge expected of KCSA candidates.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (RBAC default roles).

### kcsa-047

**Issues found:**

- **WARNING** `question_clarity` — Mojibake artifact 'â€"' appears in option B instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€"' with an em dash in option B.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. When runAsNonRoot: true is set and the container image would run as UID 0, the kubelet refuses to start the container with the error 'container has runAsNonRoot and image will run as root'. This is documented Kubernetes behavior.

**question_clarity**

The question is clear and unambiguous, asking specifically about the behavior when runAsNonRoot conflicts with a root image.

**distractor_quality**

Distractors are plausible misconceptions: A suggests a soft warning behavior, C suggests auto-remapping (a common misunderstanding), and D suggests scheduler intervention. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes the validation behavior and the actual error message. Guidance to set runAsUser is correct. Only issue is a mojibake artifact 'â€"' instead of an em dash.

**difficulty_calibration**

Medium is appropriate — requires understanding of PodSecurity validation behavior, not just definition recall.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals, covering securityContext and pod security.

### kcsa-154

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€”) appear instead of em dashes in options A and B and throughout the explanation.

**Fixes applied:**

- Replaced mojibake â€” with proper em dash — in options A, B and in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. ISO/IEC 27001 is the international standard for Information Security Management Systems (ISMS), and the mapping to Annex A controls (A.9 access control, A.10 cryptography, A.12 operations security) is accurate. The other options misrepresent ISO 27001 entirely.

**question_clarity**

The question clearly asks two things: what ISO/IEC 27001 is and which K8s controls support its Annex A. The compound nature is reasonable since the correct answer addresses both parts.

**distractor_quality**

Distractors are plausible to someone unfamiliar with ISO standards (network protocol, container image format, SLA standard) but clearly wrong to an expert. They cover common misconceptions about what ISO standards typically address.

**explanation_accuracy**

The explanation is factually accurate regarding Annex A control families and their Kubernetes mappings. Note: ISO/IEC 27001:2022 reorganized Annex A into 4 themes (not the older A.5-A.18 numbering of 27001:2013), but the legacy numbering used here is still widely referenced and recognized.

**difficulty_calibration**

Hard is appropriate — this requires knowledge of both ISO 27001 structure and Kubernetes security control mapping, which is advanced compliance content.

**domain_relevance**

Directly tests Compliance and Security Frameworks, which is the stated KCSA domain.

### kcsa-106

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in option B and the explanation where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Per Kubernetes admission webhook docs, failurePolicy: Fail causes the API server to reject requests when the webhook is unreachable or returns an error, while Ignore allows the request through. This is the definitive security-relevant behavior.

**question_clarity**

The question is clear and focused on the security implication of failurePolicy: Fail. It's unambiguous.

**distractor_quality**

Distractors are plausible: A confuses with retry semantics, C is a believable misconception about post-admission scanning, D confuses with timeoutSeconds. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

The explanation is factually accurate regarding Fail vs Ignore behavior and the security implications (attacker disabling Gatekeeper/Kyverno). However, it contains mojibake encoding artifacts (â€") where em dashes should appear.

**difficulty_calibration**

Hard is reasonable — this requires understanding webhook configuration nuances and threat modeling around policy enforcement bypass, which is beyond entry-level knowledge.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals — admission control is a core security mechanism.

### kcsa-059

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear in options A, C, and D instead of em dashes.
- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in the explanation instead of em dashes.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in options A, C, D and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. SecurityContextConstraints is indeed an OpenShift-specific admission mechanism that predates Kubernetes Pod Security Policies/Standards and controls what security context settings pods can request.

**question_clarity**

The question is clear and asks two related things: what SCC is and which distribution uses it. The phrasing is unambiguous.

**distractor_quality**

All distractors are real Kubernetes-adjacent features from major cloud providers (Azure Policy, GKE Workload Identity, EKS Pod Identity), making them plausible to someone unfamiliar with OpenShift. They are clearly wrong to an expert since none are SCC equivalents in the strict sense.

**explanation_accuracy**

The explanation is accurate: OpenShift's SCCs do predate PSP/PSS and cover capabilities, SELinux, UID ranges, volume types, etc. Mojibake artifacts (â€”) appear instead of em dashes.

**difficulty_calibration**

Medium is appropriate — recognizing SCC as an OpenShift construct requires some cross-distribution knowledge beyond core Kubernetes.

**domain_relevance**

Compliance and Security Frameworks domain is appropriate as SCC and PSS are both pod security policy frameworks.

### kcsa-076

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears in the explanation instead of an em dash '—'.

**Fixes applied:**

- Fixed mojibake encoding artifact 'â€”' → '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. The kubelet's webhook authorization mode delegates authorization decisions to the API server via SubjectAccessReview API, while AlwaysAllow grants all requests without any check. This is documented in Kubernetes kubelet authentication/authorization docs.

**question_clarity**

The question is clear, asking about both the definition of webhook mode and the security rationale for preferring it over AlwaysAllow.

**distractor_quality**

Distractors B, C, and D are plausible-sounding but factually wrong, conflating authorization with TLS/transport or audit logging concerns. They would tempt someone with surface-level knowledge.

**explanation_accuracy**

The explanation is factually accurate: it correctly identifies the --authorization-mode flag, port 10250, the SubjectAccessReview delegation, and the security risk of AlwaysAllow. However, there is a mojibake encoding artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Hard is reasonable — this requires knowledge of kubelet-specific flags, SubjectAccessReview API, and the security implications, beyond standard RBAC knowledge.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, specifically kubelet hardening which is a core KCSA topic.

### kcsa-101

**Issues found:**

- **ERROR** `explanation_accuracy` — The explanation incorrectly lists runAsUser as a pod-only field. runAsUser can be set at both pod-level and container-level; it is a shared field where container-level overrides pod-level, not a pod-only field.
- **WARNING** `distractor_quality` — Option D contains a mojibake encoding artifact 'â€"' instead of an em dash '—'.

**Fixes applied:**

- Removed runAsUser from the list of pod-only fields in the explanation, since it is a shared field settable at both levels.
- Fixed mojibake 'â€"' to em dash '—' in option D.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Pod-level securityContext (spec.securityContext) does set defaults for all containers and contains pod-only fields like fsGroup, supplementalGroups, and sysctls. Container-level securityContext overrides pod-level settings for shared fields and supports container-only fields like capabilities and privileged. This aligns with official Kubernetes documentation.

**question_clarity**

The question is clear and unambiguous, directly asking about the distinction between the two scopes of securityContext.

**distractor_quality**

Distractors A, C, and D are plausible to novices but clearly wrong to experts. D contains a mojibake artifact (â€") that should be an em dash but is still recognizable as a distractor.

**explanation_accuracy**

The explanation is largely accurate but contains a minor inaccuracy: runAsUser is NOT a pod-only field — it can be set at both pod and container level (and is one of the shared/overridable fields). Listing it as a 'pod-only field' is misleading. fsGroup, supplementalGroups, and sysctls are correctly pod-only.

**difficulty_calibration**

Medium is appropriate — this requires knowledge of specific field placement and precedence rules, beyond a simple definition.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals via securityContext, a core KCSA topic.

### kcsa-105

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts â€” appear in options A and B and in the explanation instead of em dashes.

**Fixes applied:**

- Replaced mojibake â€” with em dash — in options A, B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The Kubernetes API's list verb on Secrets returns the full SecretList including the data field with base64-encoded values, making it equivalent to reading all secret contents. Options A, C, and D all incorrectly claim list is safe or metadata-only.

**question_clarity**

The question is clear and specific, asking about the security implication of cluster-wide 'list secrets' permission. The phrasing is unambiguous.

**distractor_quality**

Distractors are well-crafted and plausible — they reflect common misconceptions that list is metadata-only (a reasonable assumption from databases/REST APIs). D in particular is a tempting trap distinguishing list vs get.

**explanation_accuracy**

The explanation is factually accurate: list does return full Secret data, base64 is encoding not encryption, and aggregated roles like edit/admin do grant these verbs. Good practical guidance about auditing RBAC bindings.

**difficulty_calibration**

Hard is appropriate — this is a subtle Kubernetes-specific behavior that surprises many practitioners and requires understanding of API semantics and RBAC implications.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (RBAC, Secrets, least privilege).

### kcsa-098

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears in the explanation where an em dash was intended.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

B is correct. RoleBinding grants permissions within a single namespace, while ClusterRoleBinding grants permissions cluster-wide. This is the fundamental distinction per Kubernetes RBAC documentation.

**question_clarity**

The question is clear and directly asks about the difference between the two binding types.

**distractor_quality**

Distractors are reasonable: A is a plausible misconception about subject types; C is a near-truth that's actually false (RoleBinding can bind ClusterRoles too); D is clearly wrong but plausible to novices. C is particularly good as a distractor since it tests a common misconception.

**explanation_accuracy**

Explanation is factually accurate — correctly notes that a RoleBinding binding a ClusterRole narrows scope to the namespace, and that ClusterRoleBinding covers cluster-scoped resources. Contains a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Easy is appropriate for this fundamental RBAC distinction commonly tested at KCSA level.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (RBAC).

### kcsa-157

**Issues found:**

- **WARNING** `question_clarity` — Option B contains a mojibake encoding artifact 'â€"' that should be an em dash (—).

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Answer B accurately describes the OpenSSF Scorecard as an automated tool that rates open-source projects' security practices, which is correct per the OpenSSF documentation. The other options are fabrications (no such CRD, no such certificate, not a CVE severity system). B is unambiguously the best answer.

**question_clarity**

The question clearly asks both what Scorecard is and how it applies to supply chain decisions. Option B addresses both parts. Phrasing is clear and grammatically sound.

**distractor_quality**

Distractors are reasonably plausible: A invokes pod security, C invokes CNCF certifications (which exist, like KCSA itself), and D mixes Scorecard with CVSS. All are clearly wrong to an expert but tempting to a novice.

**explanation_accuracy**

The explanation correctly lists Scorecard checks (branch protection, CI/CD, dependency pinning, code review, vulnerability disclosure, signed releases) and accurately ties them to evaluating Helm charts, operators, and CNI plugins. Factually accurate.

**difficulty_calibration**

Medium is appropriate — recognizing Scorecard's purpose requires familiarity with the OpenSSF ecosystem beyond basic Kubernetes knowledge but is not deeply technical.

**domain_relevance**

Directly relevant to Compliance and Security Frameworks domain, specifically supply chain security tooling which is part of KCSA scope.

### kcsa-042

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake 'â€”' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

B is correct. Sigstore keyless signing uses OIDC tokens issued by an identity provider (e.g., GitHub Actions) to authenticate to Fulcio, which issues a short-lived signing certificate; the signature and certificate are recorded in Rekor.

**question_clarity**

The question clearly asks which protocol binds CI/CD identity to a signature without managing private keys. Phrasing is precise and unambiguous.

**distractor_quality**

All distractors are plausible identity/cert mechanisms but clearly wrong: SSH CA, static PKI in a Secret, and AWS IAM role assumption are not Sigstore's mechanism. Good quality.

**explanation_accuracy**

Explanation is technically accurate — OIDC token → Fulcio short-lived cert → Rekor transparency log. Only issue is a mojibake artifact 'â€”' instead of an em dash.

**difficulty_calibration**

Hard is reasonable; this requires specific knowledge of Sigstore architecture (Fulcio, Rekor, OIDC binding) beyond basic supply-chain awareness.

**domain_relevance**

Directly relevant to Cloud Native Security overview, covering supply chain security and signing.

### kcsa-095

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in option B and explanation where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€" with proper em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The --service-account-lookup flag, when true (the default since Kubernetes 1.7+), causes the API server to validate that the ServiceAccount and its token secret still exist in etcd before authenticating a request. This means deleting the ServiceAccount invalidates its tokens.

**question_clarity**

The question is clear and directly asks what the flag does when set to true. No ambiguity.

**distractor_quality**

Distractors are plausible: external directory lookup (A), caching (C), and RBAC pre-binding (D) all sound like authentication-related behaviors but are clearly wrong to an expert. Good quality.

**explanation_accuracy**

The explanation is factually accurate regarding the flag's behavior and security implications. However, it contains mojibake encoding artifacts (â€") instead of proper em dashes, also present in option B.

**difficulty_calibration**

Hard is appropriate — this is a fairly obscure API server flag that requires deep knowledge of authentication internals.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security (kube-apiserver flags and ServiceAccount token security).

### kcsa-144

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact â€" appears in option B and explanation instead of em dash —.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Option B correctly describes projected ServiceAccount token behavior: kubelet rotates the token before expiry and apps must re-read the token file. This aligns with the BoundServiceAccountTokenVolume feature. Other options are factually wrong (90-day manual rotation, never expire, or tied to activeDeadlineSeconds).

**question_clarity**

The question is clear, asking about projected SA token expiry behavior and the required application handling. The compound question is acceptable since the answer addresses both parts.

**distractor_quality**

Distractors are plausible: A reflects legacy rotation thinking, C reflects the old non-expiring tokens behavior (pre-1.24), D conflates unrelated pod field. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

Explanation is accurate: default 1h TTL, ~80% rotation threshold, file re-read requirement, client-go handling. Contains mojibake (â€") that should be em dashes.

**difficulty_calibration**

Medium is appropriate — requires knowing projected token mechanics and rotation behavior, beyond basic SA knowledge but not deeply advanced.

**domain_relevance**

Directly tests Platform Security knowledge regarding ServiceAccount token security and rotation.

### kcsa-113

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake artifact 'â€”' appears instead of an em dash in the explanation.
- **WARNING** `explanation_accuracy` — Recommending 'failurePolicy: Fail' as a mitigation against a *malicious* webhook is misleading; Fail policy helps integrity/availability concerns but does not mitigate a compromised webhook (it actually increases the attacker's leverage by making the webhook required). Better mitigations include limiting webhook scope via objectSelector/namespaceSelector, TLS pinning via caBundle, restricting who can create/modify MutatingWebhookConfiguration objects, and monitoring webhook configurations.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash.
- Revised mitigation guidance: removed misleading 'failurePolicy: Fail' recommendation and added accurate mitigations (RBAC restrictions on webhook configuration objects, scoping via selectors, monitoring, caBundle verification).

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. A malicious admission webhook attack involves a compromised or attacker-controlled webhook server that can view sensitive data (Secrets, Pod specs) passed through admission review and/or mutate objects with malicious payloads (e.g., sidecars, env vars). This matches documented threat model concerns.

**question_clarity**

The question is direct and clear: it asks for the definition of a malicious admission webhook attack. No ambiguity.

**distractor_quality**

Distractors are plausible Kubernetes-adjacent misconceptions: brute-forcing creds, disabling webhooks via RBAC, and infinite loops. All are clearly incorrect to someone who understands webhook attack vectors but reasonable as distractors.

**explanation_accuracy**

Explanation is technically accurate regarding webhook visibility into API objects and mutation capabilities. One minor caveat: 'failurePolicy: Fail' is recommended for availability/integrity but does not directly mitigate a *malicious* webhook (it could even worsen impact). Still, the core explanation is sound. There is a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Hard is reasonable—admission webhook attack vectors are an advanced security topic typically covered in CKS/KCSA threat-model discussions.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain, specifically supply chain / admission control threats.

### kcsa-034

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact â€" appears instead of an em dash in the explanation.
- **WARNING** `question_clarity` — The phrasing 'restrict a container to a subset of the host's PID namespace' is technically inaccurate — hostPID=false gives the container its own isolated PID namespace, not a subset of the host's.

**Fixes applied:**

- Replaced mojibake â€" with proper em dash in explanation.
- Rephrased the question stem for technical accuracy — the container gets its own PID namespace rather than a 'subset' of the host's.

**Per-dimension analysis:**

**answer_correctness**

The stated answer A is correct. Setting spec.hostPID to false (the default) ensures the container does not share the host's PID namespace and cannot see host processes. hostNetwork and hostIPC concern different namespaces (network and IPC), and shareProcessNamespace concerns sharing PID namespace between containers in the same pod, not with the host.

**question_clarity**

The question is reasonably clear, asking which setting prevents a container from seeing host processes. The phrase 'restrict a container to a subset of the host's PID namespace' is slightly awkward — technically hostPID=false gives the container its OWN PID namespace, not a 'subset' of the host's — but the intent is understandable.

**distractor_quality**

Distractors are plausible: hostNetwork, hostIPC, and shareProcessNamespace are all related Pod spec security fields that learners might confuse with hostPID. They are clearly wrong to an expert since they govern different namespaces.

**explanation_accuracy**

The explanation is factually accurate: hostPID=true exposes host processes, default false isolates the container, and PSS restricts these fields. Contains a mojibake artifact â€

**difficulty_calibration**

Medium is appropriate — it requires distinguishing between several similar-sounding pod spec security fields and knowing which controls host PID namespace sharing.

**domain_relevance**

Directly relevant to Platform Security; hostPID and related fields are core Pod Security Standards concerns.

### kcsa-087

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact â€” appears in option B and the explanation instead of an em dash (—).

**Fixes applied:**

- Replaced mojibake â€” with em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. An expired root CA causes TLS verification to fail across all components (API server, kubelet, etcd, controller-manager, scheduler), and there is no automatic CA regeneration mechanism in Kubernetes — manual rotation is required. The other options describe fictional automatic behaviors.

**question_clarity**

The question is clear and direct, asking about the consequence of root CA expiry. No ambiguity.

**distractor_quality**

Distractors A and C are plausible-sounding to novices (automatic regeneration) but clearly wrong to experts. D tests understanding that TLS verification is per-handshake, not session-persistent in a way that survives expiry checks. All are appropriately misleading.

**explanation_accuracy**

The explanation is technically accurate: expired CA breaks API server↔kubelet, etcd clients, and mTLS. The kubeadm commands cited are real and correct. Only issue is the mojibake â€” which should be an em dash.

**difficulty_calibration**

Hard is reasonable — understanding CA expiry consequences and rotation procedures is an advanced topic, though one could argue medium. Not a clear mismatch.

**domain_relevance**

Directly relevant to Cluster Component Security — PKI and certificate management are core to this domain.

### kcsa-020

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€”) appear in all four options where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€” with proper em dash — in all four options.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Kubernetes Secrets are stored as base64-encoded data, which is an encoding scheme (not encryption) and is trivially reversible without any key. This aligns with official Kubernetes documentation which explicitly warns that Secrets are not encrypted by default.

**question_clarity**

The question is clear and unambiguous, asking about both the encoding mechanism and why it doesn't qualify as encryption. However, the options contain mojibake (â€”) where em dashes should appear, which is a display issue but doesn't affect comprehension.

**distractor_quality**

Distractors are well-constructed and plausible: A reflects a common misconception that Secrets are encrypted, C confuses encoding with hashing, and D represents the opposite misconception. Each tests a distinct misunderstanding.

**explanation_accuracy**

The explanation is factually accurate. Base64 is indeed trivially decoded, Secrets are not encrypted by default in etcd, and EncryptionConfiguration plus external secret managers are correct mitigations.

**difficulty_calibration**

Medium is appropriate — it requires understanding the distinction between encoding, hashing, and encryption, plus awareness of Kubernetes Secret internals. Slightly on the easier side but acceptable as medium.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals — Secret handling is a core KCSA topic.

### kcsa-110

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. Subresources like pods/exec, pods/log, pods/portforward, and pods/attach are indeed distinct API endpoints on a parent resource that require independent authorization in RBAC. This aligns with official Kubernetes RBAC documentation.

**question_clarity**

The question is clear and asks two related parts: what a subresource is and why separate permissions are needed. The options focus on the definition, which is sufficient to select A.

**distractor_quality**

Distractors are plausible misconceptions: overflow pods (B), child namespaces (C), and CRDs (D) are all Kubernetes-adjacent concepts that could confuse beginners but are clearly wrong to experts. Quality is good.

**explanation_accuracy**

The explanation is factually accurate — subresources are authorized independently and the RBAC syntax shown (resources: ["pods/exec"]) is correct. However, there is a mojibake artifact 'â€”' where an em dash should appear.

**difficulty_calibration**

Medium is appropriate; this requires understanding RBAC granularity beyond basic verbs/resources but isn't deeply complex.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals — RBAC subresource authorization is a core security concept.

### kcsa-060

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€"' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€"' with an em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The system:masters group is hardcoded in the API server's authorization layer to bypass RBAC checks, granting unconditional cluster-admin privileges. This is documented behavior and a well-known security concern.

**question_clarity**

The question is clear, well-scoped, and asks two related things: what the group grants and why it's more dangerous than the cluster-admin ClusterRoleBinding. Option B addresses both aspects directly.

**distractor_quality**

Distractors A, C, and D are plausible to someone unfamiliar with the special-cased group (sounding like generic system-level access or component identities) but clearly wrong to experts. No two options are simultaneously correct.

**explanation_accuracy**

The explanation is factually accurate: system:masters is indeed hardcoded to bypass RBAC, and membership is conveyed via the certificate's O field. The only issue is a mojibake artifact (â€") instead of an em dash.

**difficulty_calibration**

Hard is appropriate — this requires knowledge of internal API server authorization shortcuts and certificate-based group encoding, beyond standard RBAC concepts.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals, specifically authentication/authorization and privileged access risks.

### kcsa-094

**Issues found:**

- **WARNING** `question_clarity` — Mojibake artifact 'â€”' appears in option A instead of an em dash.
- **WARNING** `explanation_accuracy` — Mojibake artifact 'â€”' appears twice in the explanation instead of em dashes.

**Fixes applied:**

- Replaced mojibake 'â€”' with em dash '—' in option A.
- Replaced mojibake 'â€”' with em dash '—' in explanation (two occurrences).

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The legacy --insecure-port (8080) on kube-apiserver bypassed authentication, authorization, and TLS entirely, allowing full API access in plaintext. This was removed in 1.20 as noted.

**question_clarity**

The question clearly asks about the security risk of the former insecure port. Phrasing is understandable despite the mojibake artifact.

**distractor_quality**

Distractors A, C, and D are plausible misconceptions (metrics-only, node-to-node only, backup only) but clearly wrong to an expert. They serve their purpose adequately.

**explanation_accuracy**

Explanation is technically accurate: insecure port 8080 had no auth/authz, was removed in 1.20, and the mitigation advice is correct. Contains mojibake artifact (â€”) that should be em dash.

**difficulty_calibration**

Medium is appropriate — requires knowledge of historical apiserver flags and their security implications.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, specifically kube-apiserver hardening.

### kcsa-070

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in option B and the explanation instead of em dashes (—).
- **WARNING** `difficulty_calibration` — This is a definition-recall question with one clearly best answer; 'medium' is more appropriate than 'hard'.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and explanation.
- Lowered difficulty from hard to medium.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly defines immutable infrastructure: artifacts are replaced rather than modified in-place. This aligns with industry consensus and CNCF guidance. Other options describe related but distinct concepts (read-only filesystems, undeletable containers, IaC storage).

**question_clarity**

The question is clear and directly asks for the definition of immutable infrastructure in the container security context.

**distractor_quality**

Distractors are plausible: C (read-only filesystem) is commonly confused with immutability; D references IaC; A is a reasonable misreading of 'immutable'. All are clearly wrong to an expert.

**explanation_accuracy**

Explanation accurately describes the concept, its benefits (auditability, reproducibility, no drift), and related practices (signing, digest pinning). Contains mojibake (â€") for em dashes that should be fixed.

**difficulty_calibration**

Marked hard, but this is more of a medium-level conceptual definition question. Could be argued either way given the nuance with distractor C; acceptable but borderline.

**domain_relevance**

Directly tests Cloud Native Security overview concepts.

### kcsa-100

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€"' appears instead of em dash (—) in option A and twice in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€"' with proper em dash '—' in option A and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. allowPrivilegeEscalation controls whether a process can gain more privileges than its parent (via setuid binaries or file capabilities) and sets the no_new_privs flag. The default is indeed true when not explicitly set (though it defaults to false when privileged=false and certain conditions apply, but generally documented as true).

**question_clarity**

The question is clear and directly asks what the setting does. The mojibake em-dash (â€") in option A is a minor encoding artifact but does not impair understanding.

**distractor_quality**

Distractors B, C, and D are plausible-sounding but clearly wrong to an expert: admission controllers, PSS levels, and kubelet capability grants are unrelated to this specific field.

**explanation_accuracy**

The explanation is accurate: allowPrivilegeEscalation=false sets no_new_privs, blocks setuid escalation, and is required by PSS restricted. Contains the same encoding artifact for em-dash.

**difficulty_calibration**

Medium is appropriate — this requires understanding of Linux privilege escalation mechanics and the no_new_privs flag, beyond just memorizing a field name.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals — pod/container security context hardening.

### kcsa-081

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€"' appears in place of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€"' with an em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The Node authorizer is a special-purpose authorization mode that grants kubelets permissions only to objects related to their own node (pods scheduled on it, related secrets, configmaps, PVCs, etc.), per Kubernetes docs.

**question_clarity**

The question is clear and asks two related things: what Node authorization is and what it restricts. Both parts are answered by option B.

**distractor_quality**

Distractors are plausibly worded around node/kubelet concepts (cluster-admin, kube-scheduler, kube-proxy/firewall) but clearly wrong to those who know the Node authorizer. They serve as reasonable misdirections.

**explanation_accuracy**

Explanation is accurate: identifies the kubelet identity (system:node:<nodeName>), correctly notes pairing with RBAC and the NodeRestriction admission controller. Only issue is an encoding artifact 'â€"' instead of an em dash.

**difficulty_calibration**

Medium is appropriate — this is a specific authorization concept beyond basic RBAC but commonly covered in KCSA material.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, covering authorization modes for the API server and kubelet.

### kcsa-155

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears instead of an em dash '—' in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. kube-hunter actively probes clusters for vulnerabilities from an attacker's perspective, while kube-bench checks configuration against the CIS Kubernetes Benchmark. Both are Aqua Security tools and are widely described this way in official docs.

**question_clarity**

The question is clear and asks two related things: what kube-hunter is and how it complements kube-bench. Option B directly answers both.

**distractor_quality**

Distractors are plausible: A plays on the fact both are Aqua tools, C confuses with Falco-style runtime detection, D inverts the roles. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

The explanation is factually accurate. Only issue is a mojibake artifact 'â€”' that should be an em dash '—'.

**difficulty_calibration**

Medium is appropriate — requires knowing two specific tools and their distinct roles, not just a definition.

**domain_relevance**

Directly relevant to Compliance and Security Frameworks domain (CIS Benchmark tooling).

### kcsa-131

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in option B instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Option B correctly defines eBPF as a Linux kernel technology for running sandboxed programs and accurately describes its use by Falco (syscall monitoring) and Cilium (network policy enforcement). The other options are factually wrong: eBPF is not a firewall protocol, CRD, or packet format.

**question_clarity**

The question clearly asks what eBPF is and how it's used by named security tools. Stem is unambiguous.

**distractor_quality**

Distractors are plausible to novices (firewall protocol, CRD, packet format all sound K8s-related) but clearly wrong to experts. Quality is appropriate.

**explanation_accuracy**

Explanation accurately covers Falco's syscall monitoring via eBPF, Cilium's L3/L4/L7 networking, and Tetragon's runtime security. The mention of the eBPF verifier for kernel safety is correct.

**difficulty_calibration**

Medium is appropriate — requires knowing both eBPF fundamentals and specific tool implementations.

**domain_relevance**

Directly relevant to Platform Security, covering kernel-level security tooling commonly used in Kubernetes.

### kcsa-022

**Issues found:**

- **WARNING** `explanation_accuracy` — Explanation lists 'runAsNonRoot' and 'non-root runAsUser' as separate requirements when they overlap. Also, restricted permits NET_BIND_SERVICE capability to be added back after drop ALL, so 'dropping ALL capabilities' is slightly imprecise.

**Fixes applied:**

- Clarified capabilities rule (drop ALL, only NET_BIND_SERVICE may be added) and removed redundant non-root listing.

**Per-dimension analysis:**

**answer_correctness**

Option A correctly captures two key requirements of the restricted Pod Security Standard: runAsNonRoot and dropping ALL capabilities (except NET_BIND_SERVICE). While restricted enforces more than just these two items, the question asks for the MOST accurate among the choices, and A is clearly the only correct option.

**question_clarity**

The question is clear and the 'Select the MOST accurate' qualifier appropriately signals that A is a partial but best representation of restricted's requirements. No ambiguity.

**distractor_quality**

Distractors are plausible misconceptions: B confuses PSS with runtime choice, C contradicts itself (restricted explicitly forbids privileged), and D is a common misunderstanding about namespace-based restrictions. All are clearly wrong to an expert.

**explanation_accuracy**

Explanation is largely accurate but contains a minor redundancy/imprecision: it lists both 'runAsNonRoot' and 'a non-root runAsUser' as separate items when they are essentially the same control. Also, restricted allows the NET_BIND_SERVICE capability to be retained — 'dropping ALL capabilities' is technically 'drop ALL' with optional add of NET_BIND_SERVICE only. Minor but worth noting.

**difficulty_calibration**

Medium is appropriate — requires knowing the three PSS levels and the specific controls of restricted, which is beyond beginner recall but not deeply nuanced.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (Pod Security Standards), which is core KCSA content.

### kcsa-051

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear instead of em dashes in options A and B.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in options A and B.

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. A container escape is a process breaking out of container isolation to access the host OS, and privileged containers/dangerous capabilities (like CAP_SYS_ADMIN) are the most common enablers per Kubernetes security documentation and CNCF guidance.

**question_clarity**

The question is clear and asks for both the definition and the most common precondition, which option B addresses directly.

**distractor_quality**

Distractors A, C, and D describe unrelated Kubernetes concepts (TTL controllers, image pulls, eviction) that are plausible to a novice but clearly not security escape scenarios. They are appropriately wrong.

**explanation_accuracy**

The explanation accurately describes container escape vectors (privileged mode, capabilities, hostPath mounts, kernel CVEs) and provides correct mitigations. Technically sound.

**difficulty_calibration**

Medium is appropriate — requires understanding container isolation, capabilities, and threat modeling concepts beyond basic knowledge.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain, specifically container/host boundary threats.

### kcsa-079

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact â€" appears in both option A and the explanation instead of an em dash (—).

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option A and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Bootstrap tokens are short-lived credentials used by kubeadm join for TLS bootstrapping, and best practice is to delete them after use. The other options misrepresent bootstrap tokens.

**question_clarity**

The question is clear, asking both what bootstrap tokens are and what security consideration applies post-setup. Option B addresses both parts.

**distractor_quality**

Distractors are plausible: A confuses bootstrap with static node tokens, C confuses with admin credentials, D confuses with OIDC JWTs. All clearly wrong to an expert but believable to a novice.

**explanation_accuracy**

Explanation is technically accurate — bootstrap tokens are indeed stored as Secrets of type bootstrap.kubernetes.io/token in kube-system, with default 24h TTL, and should be deleted after node join. Contains mojibake encoding artifacts (â€") that should be em dashes.

**difficulty_calibration**

Medium is appropriate — requires knowledge of kubeadm bootstrapping internals beyond basic concepts.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security, covering authentication tokens used during cluster bootstrap.

### kcsa-109

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€”) appear in option B and the explanation where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€” with proper em dash — in option B and explanation.
- Clarified the 'bind' verb statement to reference RoleBinding/ClusterRoleBinding creation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The 'escalate' verb on roles/clusterroles allows a user to create or update Roles/ClusterRoles with permissions beyond what they themselves hold, bypassing the default RBAC privilege escalation prevention check documented in the Kubernetes RBAC docs.

**question_clarity**

The question clearly asks two things: what the 'escalate' verb does and why it is dangerous. Both are addressed by option B.

**distractor_quality**

Distractors are plausible-sounding misdirections (incident response escalation, PodSecurity enforcement modes, security committee ticketing) that would tempt someone unfamiliar with RBAC verbs but are clearly wrong to an expert.

**explanation_accuracy**

The explanation is technically accurate: it correctly describes the privilege-escalation prevention behavior, the bypass via 'escalate', and notes the analogous 'bind' verb for RoleBindings/ClusterRoleBindings. Minor imprecision: 'bind' applies to roles being bound (not literally 'on clusterrolebindings'), but the gist is correct. Mojibake artifacts (â€”) appear instead of em dashes.

**difficulty_calibration**

Hard is appropriate; the 'escalate' verb is a niche RBAC concept that typically only advanced practitioners and CKS-level candidates know.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (RBAC privilege escalation), which is in scope for KCSA.

### kcsa-126

**Issues found:**

- **ERROR** `answer_correctness` — Answer B claims /api and /apis are accessible without authentication by default. In Kubernetes with default RBAC, the system:discovery ClusterRole (which covers /api and /apis) is bound to system:authenticated, not system:anonymous. Only /version, /healthz, /livez, /readyz are anonymously accessible via system:public-info-viewer.
- **ERROR** `explanation_accuracy` — Explanation incorrectly states system:anonymous gets the system:discovery ClusterRole. system:discovery is bound to system:authenticated only. Anonymous discovery is gated by system:public-info-viewer, which does not include /api or /apis.
- **WARNING** `distractor_quality` — Options A (/healthz) and D (/readyz) are also accessible anonymously by default via system:public-info-viewer, so calling them wrong is itself inaccurate — they reveal what they claim. The question needs a reframing to have a single unambiguous best answer.
- **WARNING** `explanation_accuracy` — Mojibake artifacts â€" appear in options A, B and the explanation where em dashes were intended.

**Fixes applied:**

- Reframed question to focus on which endpoint leaks the Kubernetes version (the primary CVE-targeting concern) so a single best answer exists.
- Corrected the RBAC mechanism: anonymous access is granted via system:public-info-viewer, not system:discovery.
- Removed the incorrect claim that /api and /apis are anonymously accessible by default.
- Replaced mojibake â€" with proper em dashes.

**Per-dimension analysis:**

**answer_correctness**

The stated answer B is partially incorrect for modern Kubernetes (1.6+ with RBAC enabled by default). The system:public-info-viewer ClusterRole (bound to both system:authenticated AND system:unauthenticated) grants anonymous access to /version, /healthz, /livez, and /readyz only. The system:discovery ClusterRole — which exposes /api, /apis, /openapi — is bound only to system:authenticated, NOT system:anonymous. So /api and /apis are NOT accessible without authentication by default in current Kubernetes versions; only /version (and health endpoints) leak to anonymous callers.

**question_clarity**

The question is clear in intent — it asks which endpoints are reachable without authentication and what they expose. Phrasing is grammatical and unambiguous.

**distractor_quality**

Distractors A, C, D each reference real endpoints and are plausible. However, A (/healthz) is actually also accessible anonymously by default via system:public-info-viewer, which makes it partially correct and undermines the single-best-answer structure. C (/metrics) requires auth, so it is a clean distractor. D (/readyz) is also anonymously accessible via system:public-info-viewer, creating a second partially-correct option.

**explanation_accuracy**

The explanation contains a factual error: it claims system:anonymous receives the system:discovery ClusterRole. In default RBAC, system:discovery is bound to system:authenticated only. Anonymous users get system:public-info-viewer (which excludes /api and /apis). The mitigation advice (--anonymous-auth=false) is correct, but the underlying mechanism is misstated. Also contains mojibake (â€") instead of em dashes.

**difficulty_calibration**

Medium is appropriate — this requires knowing both the endpoints and the default RBAC bindings around anonymous access, which is more than recall but not deeply scenario-based.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, specifically reconnaissance/information disclosure via the API server.

### kcsa-099

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears instead of an em dash.
- **WARNING** `explanation_accuracy` — Statement that PSS 'baseline' only 'restricts dangerous paths' is inaccurate; the baseline profile disallows hostPath volumes entirely (both baseline and restricted forbid hostPath).

**Fixes applied:**

- Fixed mojibake â€” → —.
- Corrected PSS baseline statement: both baseline and restricted profiles disallow hostPath volumes.

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. HostPath volumes mount host node filesystem directories into pods, which can expose sensitive files (e.g., /etc/kubernetes/pki, kubelet credentials) or enable container escape via sockets like /var/run/docker.sock. This aligns with Kubernetes documentation and Pod Security Standards which restrict HostPath in baseline/restricted profiles.

**question_clarity**

The question is clear and directly asks about the security risk of HostPath volumes. No ambiguity.

**distractor_quality**

Distractors A, C, D are plausible-sounding but clearly wrong to someone with HostPath knowledge. They reference real Kubernetes concepts (DoS, quotas, scheduling) but none describe HostPath risks accurately.

**explanation_accuracy**

Explanation is technically accurate regarding HostPath risks and PSS handling. However, there is a mojibake artifact 'â€”' that should be an em dash. Also, a minor nuance: PSS baseline disallows hostPath entirely (not just restricts dangerous paths) — the baseline profile forbids hostPath volumes outright per official PSS documentation.

**difficulty_calibration**

Medium is appropriate — requires understanding of volume types and security implications beyond beginner level.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals, a core KCSA domain topic.

### kcsa-118

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly describes DaemonSet-based persistence: attackers deploy a malicious DaemonSet to run a backdoor on every node, which auto-restarts and propagates to new nodes. This matches MITRE ATT&CK for Containers (T1543) and documented Kubernetes threat patterns.

**question_clarity**

The question clearly asks for the definition of a specific persistence technique. The phrasing is unambiguous and well-scoped.

**distractor_quality**

Distractors are plausible: A (reducing replicas) is wrong because DaemonSets don't use replica counts; C (resource exhaustion) describes DoS not persistence; D (init container injection) is a real technique but not the canonical 'DaemonSet-based persistence' pattern. All require domain knowledge to eliminate.

**explanation_accuracy**

The explanation is technically accurate regarding DaemonSet behavior, attacker motives, detection (Falco/audit), and mitigation (RBAC). However, it contains a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Hard is reasonable since it requires knowing both Kubernetes DaemonSet semantics and adversary persistence techniques, though one could argue medium. Not a clear mismatch.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, specifically persistence tactics post-compromise.

### kcsa-032

**Issues found:**

- **ERROR** `explanation_accuracy` — The statement 'Since Kubernetes 1.27, RuntimeDefault is graduated to stable' is incorrect. Seccomp support (seccompProfile field) graduated to GA in v1.19. What graduated to stable in v1.27 was the SeccompDefault feature gate that allows the kubelet to use RuntimeDefault as the default seccomp profile for all workloads.
- **WARNING** `explanation_accuracy` — The parenthetical '(Unconfined, Localhost, RuntimeDefault)' is presented as if these are alternative profile names, but they are the values of the 'type' field within seccompProfile. The phrasing is confusing.

**Fixes applied:**

- Corrected the inaccurate claim about 1.27 graduation—clarified that seccompProfile field went GA in 1.19, while the SeccompDefault kubelet feature (making RuntimeDefault the default) graduated to stable in 1.27.
- Clarified that Unconfined, RuntimeDefault, and Localhost are values of the seccompProfile.type field, not separate profiles.

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. Seccomp (Secure Computing Mode) profiles filter Linux system calls available to a container process. The other options describe unrelated concepts (network policy, resource quota, RBAC).

**question_clarity**

The question is clear and direct, asking for the definition and scope of seccomp profiles in Kubernetes.

**distractor_quality**

Distractors are plausible Kubernetes-adjacent concepts (NetworkPolicy, ResourceQuota, RBAC) that a novice might confuse with seccomp, but are clearly distinguishable to an informed test-taker.

**explanation_accuracy**

The core explanation about seccomp filtering syscalls is accurate. However, the parenthetical '(Unconfined, Localhost, RuntimeDefault)' is mislabeled as profile 'types' when they're actually the 'type' field values for seccompProfile. Also, the claim 'Since Kubernetes 1.27, RuntimeDefault is graduated to stable' is inaccurate—seccomp support (the seccompProfile field) graduated to GA in Kubernetes 1.19. What graduated later (1.27) was the SeccompDefault kubelet feature gate enabling RuntimeDefault as the default for all workloads. This is misleading.

**difficulty_calibration**

Medium is reasonable; it tests conceptual understanding beyond pure definition recall but doesn't require deep configuration knowledge. Could arguably be easy, but medium is acceptable.

**domain_relevance**

Seccomp is a core Platform Security topic directly relevant to KCSA.

### kcsa-160

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear in options A, C, D and in the explanation instead of em dashes.

**Fixes applied:**

- Replaced â€" with — (em dash) in options A, C, D and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. SecurityContextConstraints is indeed an OpenShift-specific admission mechanism that predates Kubernetes Pod Security Standards (and the earlier PodSecurityPolicy) and influenced their design. The other options are unrelated cloud-provider features.

**question_clarity**

The question clearly asks what SCC is and how it relates to PSS. Phrasing is straightforward.

**distractor_quality**

Distractors A, C, and D are plausible cloud-provider security/identity features that a less-informed test-taker might confuse, while clearly wrong to an expert. They serve their role well.

**explanation_accuracy**

The explanation is factually accurate: SCCs in OpenShift control pod security contexts, predate PSS, and are more granular (capabilities, SELinux, UID ranges, volume types). KCSA relevance note is reasonable.

**difficulty_calibration**

Medium is appropriate — requires knowledge of OpenShift-specific tooling and its relation to upstream Kubernetes PSS.

**domain_relevance**

Directly relevant to Compliance and Security Frameworks domain, covering cross-platform security implementations.

### kcsa-074

**Issues found:**

- **WARNING** `question_clarity` — Option B contains a mojibake encoding artifact 'â€"' instead of an em dash (—).

**Fixes applied:**

- Replaced mojibake 'â€"' with an em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly identifies the CNCF Cloud Native Security Whitepaper as a comprehensive security guidance document covering lifecycle, supply chain, runtime security, and compliance. This aligns with the actual v2 (2022) whitepaper published by the CNCF Security TAG and is a recognized KCSA reference.

**question_clarity**

The question is clear and asks two related things: what the document is and why it matters for KCSA. The answer choices align with this dual ask.

**distractor_quality**

Distractors are plausible: A is clearly wrong but tempting to a novice; C confuses it with exam curriculum; D conflates it with TAG audit processes. All are distinguishable by an informed candidate.

**explanation_accuracy**

The explanation is accurate — the whitepaper v2 was published in 2022 by the CNCF Security TAG and covers the listed pillars. Mapping to KCSA domains is correct.

**difficulty_calibration**

Medium is appropriate; recognizing the whitepaper and its scope requires some familiarity beyond beginner level but isn't deeply technical.

**domain_relevance**

Directly tests Overview of Cloud Native Security knowledge, as the whitepaper is a foundational reference for that domain.

### kcsa-028

**Issues found:**

- **ERROR** `question_clarity` — The stem 'if left with default permissions' is factually wrong — none of the options describe Kubernetes default permissions. D and C are explicit misconfigurations that an admin must create; the 'view' ClusterRole by default excludes secrets; system:discovery only grants discovery API access by default. The stem should ask about misconfigured/overly-permissive RBAC.
- **WARNING** `distractor_quality` — Option C (cluster-admin bound to system:authenticated) is nearly as correct as D, because all ServiceAccounts are members of system:authenticated and would inherit cluster-admin, allowing any pod to read secrets. This creates ambiguity between two correct answers.

**Fixes applied:**

- Rewrote stem to ask about a misconfigured/overly-permissive RBAC binding rather than 'default permissions'.
- Replaced ambiguous option C with a clearly-wrong-but-plausible distractor (Role 'secret-reader' in a single namespace) to eliminate the dual-correct-answer issue.
- Expanded explanation to clarify why other options are insufficient or inaccurate.

**Per-dimension analysis:**

**answer_correctness**

Answer D is the best choice as it most directly describes a misconfiguration where every pod (via its ServiceAccount in the system:serviceaccounts group) gains cluster-admin and can read all secrets. However, option C (cluster-admin bound to system:authenticated) is also catastrophic and would similarly grant pods these rights since ServiceAccounts authenticate and are members of system:authenticated — making C nearly as correct. D is slightly more precise to the 'pod' framing, so it remains defensible as the best answer.

**question_clarity**

The phrase 'if left with default permissions' is misleading and inaccurate. None of the listed options represent Kubernetes default configurations — D and C are explicit misconfigurations an administrator would have to create, the built-in 'view' ClusterRole deliberately excludes secrets, and system:discovery only grants discovery endpoints. The stem should ask about dangerous/misconfigured RBAC bindings, not 'default permissions'.

**distractor_quality**

Distractors are mostly plausible: A (view ClusterRole) is a common misconception since people assume 'view' includes secrets, and B (system:discovery) tests knowledge of built-in bindings. However, C is too close to D in correctness — both describe catastrophic over-binding of cluster-admin and both would allow pods to read all secrets, weakening the single-best-answer structure.

**explanation_accuracy**

The explanation is factually accurate: binding cluster-admin to system:serviceaccounts does grant every pod cluster-admin, and the remediation guidance (bind to specific named ServiceAccounts) is correct. It does not, however, address why C is not the intended answer, which would help disambiguate.

**difficulty_calibration**

Medium is appropriate. The question requires understanding RBAC groups (system:serviceaccounts vs system:authenticated), the scope of built-in ClusterRoles like 'view', and threat modeling of pod compromise — beyond beginner level but not deeply technical.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain, specifically the 'Persistence' and 'Privilege Escalation' threat categories involving overly permissive RBAC that enables lateral movement and secret exfiltration from compromised pods.

### kcsa-025

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Container escape is correctly classified as Elevation of Privilege in STRIDE — the attacker gains higher privileges (host access) than the container sandbox intended. This aligns with Microsoft's STRIDE framework and cloud-native threat modeling guidance.

**question_clarity**

The question is clear and unambiguous, presenting a specific scenario (container escape to host) and asking for its STRIDE category.

**distractor_quality**

Distractors are plausible: Tampering could be confused since the attacker affects the host, Information Disclosure since escape may reveal data, and Spoofing is a weaker but recognizable STRIDE category. All are clearly wrong to someone who knows STRIDE definitions.

**explanation_accuracy**

The explanation correctly defines all six STRIDE categories and correctly classifies container escape as EoP. Contains a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Medium is appropriate — requires knowing both STRIDE categories and how container escape maps to privilege gain.

**domain_relevance**

Directly tests Kubernetes Threat Model domain knowledge (STRIDE applied to container security).

### kcsa-153

**Issues found:**

- **WARNING** `explanation_accuracy` — The 2019 CNCF-commissioned Kubernetes security audit was performed by Trail of Bits and Atredis Partners, not Trail of Bits and NCC Group. NCC Group has conducted other CNCF audits but the explanation conflates them.

**Fixes applied:**

- Corrected audit firm attribution: 2019 audit was Trail of Bits + Atredis Partners; NCC Group has performed other CNCF security audits.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. CNCF commissioned the 2019 Kubernetes security audit performed by Trail of Bits and Atredis Partners, and additional audits have been performed by NCC Group (e.g., the 2023 audit of related components). These are the well-known official third-party audits referenced in CNCF documentation.

**question_clarity**

The question is clear and unambiguous, asking which organization conducted official security audits of Kubernetes. The stem provides enough context for a knowledgeable candidate.

**distractor_quality**

Distractors are plausible: CIS does produce benchmarks (but not codebase audits), PSC handles vulnerabilities (not audits), and NIST is a believable but incorrect option. All are clearly wrong to an expert while tempting to novices.

**explanation_accuracy**

The explanation is largely accurate but slightly imprecise — the 2019 audit was conducted by Trail of Bits and Atredis Partners (not NCC Group alone); NCC Group performed later audits of related projects. The mention of TokenRequest/BoundServiceAccountTokenVolume as outcomes is reasonable context. Minor inaccuracy but not misleading regarding the main point.

**difficulty_calibration**

Hard is reasonable; knowing the specific firms that conducted Kubernetes audits is detailed trivia not typically covered in introductory material.

**domain_relevance**

Directly tests knowledge of compliance and security frameworks/audits relevant to KCSA.

### kcsa-096

**Issues found:**

- **WARNING** `question_clarity` — Option D contains a mojibake encoding artifact 'â€"' instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option D.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Secrets mounted as volumes do support file permissions (defaultMode/mode) and are updated by the kubelet without pod restart, while env vars require restart and can leak via process inspection. This aligns with official Kubernetes Secret documentation.

**question_clarity**

The question is clear and unambiguous, asking specifically about the security difference between the two consumption methods.

**distractor_quality**

Distractors are plausible misconceptions: A reflects a common false belief about env var encryption, C inverts reality about rotation, and D is a tempting 'no difference' option. All are clearly wrong to an informed reader.

**explanation_accuracy**

Explanation is accurate: env vars are visible in /proc/<pid>/environ, propagate to child processes, and require restart for updates; volume-mounted secrets are refreshed by kubelet (default sync ~1 minute) and support file mode permissions.

**difficulty_calibration**

Medium is appropriate — requires understanding of two Secret consumption patterns and their operational/security tradeoffs, beyond pure definitions.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals, specifically Secret handling best practices.

### kcsa-134

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€"' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€"' with proper em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. RuntimeClass is indeed a Kubernetes resource (cluster-scoped) that selects the container runtime configuration to be used to run a pod's containers, referenced via spec.runtimeClassName. This aligns with official Kubernetes documentation.

**question_clarity**

The question is clear and asks two related things: what RuntimeClass is and how it enforces sandbox runtimes. Option B directly addresses the definition and reference mechanism.

**distractor_quality**

Distractors are plausible: A confuses with PriorityClass, C invents an RBAC concept, D conflates with seccomp/PSA-like CRDs. All are clearly wrong to an expert but tempting to novices.

**explanation_accuracy**

Explanation is factually accurate: gVisor uses the runsc handler via containerd, and admission controllers can enforce RuntimeClass usage. Only issue is a mojibake artifact 'â€"' instead of an em dash.

**difficulty_calibration**

Medium is appropriate — RuntimeClass is an intermediate KCSA/CKS topic involving runtime isolation knowledge beyond basics.

**domain_relevance**

Directly relevant to Platform Security, specifically sandboxed runtimes and multi-tenant isolation.

### kcsa-011

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The NodeRestriction admission controller limits kubelets to modifying only their own Node object and Pod objects bound to themselves, per official Kubernetes documentation.

**question_clarity**

The question is clear and directly asks about the function of the NodeRestriction admission controller.

**distractor_quality**

Distractors are plausible Kubernetes-sounding restrictions (pod limits, NotReady handling, namespace access) that could mislead someone unfamiliar with this specific admission controller. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation is factually accurate regarding the controller's behavior and the security rationale (blast radius reduction). Contains a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Hard is slightly aggressive for this — it's a single-concept recall question about an admission controller. Medium would fit better, but hard is defensible given NodeRestriction is an advanced security topic. Not a clear mismatch.

**domain_relevance**

Directly relevant to Kubernetes Cluster Component Security; NodeRestriction is a core kubelet/API server security mechanism.

### kcsa-125

**Issues found:**

- **WARNING** `question_clarity` — Stem asks for a 'resource' but correct option B is an RBAC permission, not a resource. Rewording the stem to ask about which permission/capability an attacker needs would align with answer B.
- **WARNING** `distractor_quality` — Options C and D contain mojibake encoding artifacts (â€") instead of em dashes.

**Fixes applied:**

- Reworded stem to ask about the RBAC permission rather than 'resource', aligning with answer B.
- Fixed mojibake â€" → — in options C and D.
- Tightened option A to be a clearer distractor.

**Per-dimension analysis:**

**answer_correctness**

The intended answer B is conceptually right (an attacker with list/watch on pods at cluster scope can enumerate all pods), but B describes an RBAC permission, not a 'Kubernetes resource' as the question asks. Strictly read, the resource that reveals all pods when listed cluster-wide is the 'pods' resource itself — no option states that cleanly. This creates ambiguity between B (the permission/grant) and a hypothetical 'pods' answer.

**question_clarity**

The stem asks for a 'Kubernetes resource ... if attackers can list at the cluster scope' but the correct option B describes a ClusterRole granting list/watch on pods — i.e., a permission rather than the resource being listed. This mismatch between stem ('resource') and answer ('ClusterRole with permissions') is confusing.

**distractor_quality**

Options C and D contain mojibake (â€") instead of em dashes. Option A ('ClusterRole view binding') is a weak distractor — it's not even a coherent resource name. C is somewhat plausible since nodes do reference pods via describe output, but the nodes API object doesn't embed pod lists. Distractors could be stronger.

**explanation_accuracy**

The explanation is factually accurate: cluster-scoped pod list permission does reveal namespace, node, labels, images, and ServiceAccount info, and scoping via namespaced Roles is best practice. No factual errors.

**difficulty_calibration**

Medium is reasonable — it requires understanding RBAC scope semantics and reconnaissance implications, beyond pure recall but not deeply nuanced.

**domain_relevance**

Directly relevant to the Kubernetes Threat Model domain (reconnaissance via excessive RBAC permissions).

### kcsa-062

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear in options A, B, C, and D instead of em dashes.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in all four options.

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. The CIA triad universally refers to Confidentiality, Integrity, and Availability — the foundational security model taught in every security curriculum and applied to Kubernetes via the examples given.

**question_clarity**

The question is clear and asks for both definition and application. Options provide a definition with brief application context, matching the question's two-part nature.

**distractor_quality**

Distractors A, C, and D are plausible-sounding fabrications using Kubernetes terminology (image admission, architecture layers, attack surfaces) that could tempt someone unfamiliar with general security concepts. Quality is appropriate for an easy question.

**explanation_accuracy**

The explanation correctly maps each CIA property to concrete Kubernetes controls: RBAC/Secrets/mTLS for confidentiality, signing/admission/audit for integrity, and limits/PDBs/autoscaling for availability. All mappings are accurate.

**difficulty_calibration**

Easy is appropriate — the CIA triad is foundational security knowledge expected at the KCSA level.

**domain_relevance**

Directly relevant to 'Overview of Cloud Native Security' as the CIA triad is the foundational security model.

### kcsa-128

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear in options A and D where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in options A and D.

**Per-dimension analysis:**

**answer_correctness**

Option B correctly identifies the primary security risks of co-locating dev/staging/prod environments via namespaces in a shared cluster: RBAC misconfigurations, NetworkPolicy gaps, and quota issues that can allow cross-environment access. This aligns with CNCF and Kubernetes documentation that namespaces are a soft isolation boundary, not a security boundary. A is wrong (namespaces ≠ cluster isolation), C is irrelevant, and D dismisses real security implications.

**question_clarity**

The phrase 'sharing Kubernetes namespaces across different environments' is slightly ambiguous — it could mean using one namespace for all environments, or using a single cluster with separate namespaces per environment. Context from option B clarifies the intended meaning (multi-tenancy via namespaces in one cluster), but the stem could be sharper.

**distractor_quality**

Distractors are reasonably plausible: A reflects a common misconception that namespaces equal isolation, C is a believable but irrelevant security concern, and D represents the dismissive view. All are clearly wrong to an expert. Quality is acceptable.

**explanation_accuracy**

The explanation accurately captures the risks (RBAC overreach, NetworkPolicy gaps, secret leakage) and correctly identifies separate clusters as hard isolation while per-namespace controls provide softer isolation. This matches Kubernetes multi-tenancy guidance.

**difficulty_calibration**

Hard is slightly aggressive — the concept is more medium-level since the correct answer is the only one that mentions concrete security mechanisms. However, recognizing that namespaces are not a strong security boundary is a nuanced KCSA topic, so hard is defensible.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain, specifically multi-tenancy/isolation threats — clearly relevant to KCSA.“},

### kcsa-158

**Issues found:**

- **ERROR** `explanation_accuracy` — The explanation describes SLSA's original 4-level model (0.1 draft), but SLSA v1.0 (current standard since 2023) restructured into tracks with Build levels L1-L3 only — Level 4 was removed. Learners would form an outdated mental model.
- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in option B instead of an em dash.
- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in the explanation instead of an em dash.

**Fixes applied:**

- Updated explanation to reflect SLSA v1.0 (current) three Build levels instead of the deprecated 4-level model.
- Fixed mojibake 'â€”' → '—' in option B.
- Fixed mojibake 'â€”' → '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

B is correct. SLSA is indeed a security framework with integrity levels for software build processes, and higher levels provide stronger supply chain guarantees. The other options are factually incorrect: SLSA is not a storage class, not a replacement for Cosign (it complements signing tools), and not a network policy standard.

**question_clarity**

The question is clear, asking both what SLSA is and how it relates to Kubernetes image security. The two-part nature is acceptable since option B addresses both aspects.

**distractor_quality**

Distractors are plausible to someone unfamiliar with SLSA. A references storage (wrong domain), C confuses SLSA with signing tools like Cosign (a common conflation), and D mixes network policies with CI/CD. All are clearly wrong to an expert but believable to a novice.

**explanation_accuracy**

The explanation has a notable accuracy issue: SLSA v1.0 (released 2023) restructured to three tracks/levels (Build L1-L3) and removed the original Level 4. The four-level model described (L1 documented, L2 build service, L3 source/build integrity, L4 hermetic/reproducible) reflects the older SLSA 0.1 draft. This is outdated and could mislead learners. The admission controller and SolarWinds context is accurate.

**difficulty_calibration**

Hard is appropriate. SLSA is a specialized supply chain security topic requiring knowledge beyond basic Kubernetes, including attestations and admission control integration.

**domain_relevance**

Directly tests Compliance and Security Frameworks knowledge, specifically supply chain security standards relevant to Kubernetes image security.

### kcsa-136

**Issues found:**

- **WARNING** `question_clarity` — Options A, B, and D contain mojibake 'â€"' where an em dash '—' should appear, due to a character encoding issue.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in options A, B, and D.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. TLS termination at the Ingress controller decrypts traffic, and backend traffic is unencrypted by default unless additional measures (mTLS, service mesh, or re-encryption) are configured. This aligns with Kubernetes Ingress documentation.

**question_clarity**

The question clearly asks for both the definition of TLS termination and its security implication. The phrasing is unambiguous.

**distractor_quality**

Distractors are plausible: A describes re-encryption (a real pattern but not default), C describes TLS passthrough (a real alternative mode), and D is a common misconception. All are clearly wrong to an expert.

**explanation_accuracy**

The explanation accurately describes default behavior, mentions mTLS/service mesh and passthrough as mitigations, and correctly notes backend traffic exposure on the pod network.

**difficulty_calibration**

Medium is appropriate — requires understanding of Ingress behavior plus security implications, beyond a simple definition.

**domain_relevance**

Directly tests Platform Security knowledge regarding Ingress TLS handling.});  _plac_placeholder_marker_to_be_ignored_NA NA.

The options contain mojibake (â€") instead of an em dash.

(Note: ignore the stray placeholder text in the previous sentence; the real issue is the encoding artifact.)

### kcsa-045

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts (â€") appear in all four options instead of em dashes (—).

**Fixes applied:**

- Replaced mojibake â€" with proper em dash — in all four options.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. --service-account-key-file specifies the file containing PEM-encoded x509 RSA or ECDSA public/private keys used to verify ServiceAccount tokens, and the corresponding private key (--service-account-signing-key-file) signs them. Compromise of the signing key allows forging tokens for any ServiceAccount.

**question_clarity**

The question is clear and asks two related parts: what the flag configures and the security implication of private key compromise. Phrasing is unambiguous.

**distractor_quality**

Distractors are plausible (etcd encryption, TLS, Secrets encryption are all real Kubernetes security concerns) but clearly wrong to someone who knows the flag's purpose. Good quality.

**explanation_accuracy**

The explanation accurately describes the flag's purpose, the related signing flag, and the consequences of key compromise. Technically correct.

**difficulty_calibration**

Hard is appropriate — this requires detailed knowledge of specific kube-apiserver flags and the ServiceAccount token signing/verification mechanism.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically kube-apiserver configuration flags and their security implications.

### kcsa-068

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in option B instead of an em dash.
- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears in explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake â€” with em dash — in option B.
- Replaced mojibake â€” with em dash — in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The Pod Security Admission 'warn' mode allows pods to be created but returns warnings to the user (via the kubectl/API client), which is documented behavior per Kubernetes Pod Security Standards docs.

**question_clarity**

The question is clear and directly asks about the purpose of the 'warn' mode in PSA.

**distractor_quality**

Distractors are plausible: A misinterprets 'warn' as alerting administrators, C confuses it with enforce, D plays on the SIGTERM/warn association. All are clearly wrong to someone who knows PSA.

**explanation_accuracy**

Explanation accurately describes the three PSA modes (enforce, audit, warn) and their purpose, including the migration use case. Contains mojibake (â€”) where em dashes should be.

**difficulty_calibration**

Medium is appropriate — it requires knowing the three PSA modes and their specific semantics, which is beyond a beginner-level definition.

**domain_relevance**

Directly relevant to Cloud Native Security overview, covering Kubernetes admission controls.

### kcsa-151

**Issues found:**

- **WARNING** `explanation_accuracy` — The explanation says 'NIST CSF 2.0 functions' but lists only five (Identify, Protect, Detect, Respond, Recover). CSF 2.0 (released Feb 2024) added a sixth function, 'Govern', which is omitted. The five listed are correct for CSF 1.1, but labeling them as 2.0 is misleading.

**Fixes applied:**

- Clarified CSF version and noted that CSF 2.0 added the Govern function.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly identifies NIST CSF as a risk management framework with the five functions: Identify, Protect, Detect, Respond, Recover. This is the official NIST definition (CSF 2.0 adds Govern as a sixth function but the original five remain). The mapping to Kubernetes security controls is accurate.

**question_clarity**

The question clearly asks about NIST CSF and its mapping to Kubernetes security. Phrasing is unambiguous and grammatically correct.

**distractor_quality**

Distractor A (export controls) is plausible since NIST does have some role in standards but clearly wrong. C (NIST-managed K8s distro) is plausible-sounding but false. D (exclusively US gov) is a common misconception worth dispelling. All distractors are reasonable.

**explanation_accuracy**

The explanation correctly lists the five functions and provides accurate Kubernetes mappings: inventory for Identify, PSS/RBAC/NetworkPolicy for Protect, Falco/audit logs for Detect, runbooks for Respond, etcd backups for Recover. Note CSF 2.0 actually added a sixth function 'Govern', but the explanation references 'NIST CSF 2.0 functions' while only listing five — this is a minor inaccuracy worth noting.

**difficulty_calibration**

Medium is appropriate; requires knowledge of both NIST CSF and Kubernetes security control mapping.

**domain_relevance**

Directly tests Compliance and Security Frameworks domain.

### kcsa-063

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts â€” appear in option A and in the explanation instead of em dashes.

**Fixes applied:**

- Replaced mojibake â€” with proper em dash — in option A and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly defines an OCI image digest as a SHA256 hash of the image manifest, and accurately explains why this is more secure than mutable tags. This matches the OCI distribution specification.

**question_clarity**

The question is clear, asking for both the definition and security benefit of digests vs tags.

**distractor_quality**

Distractors are plausible: A confuses digests with tags, C confuses digests with signatures (cosign), D confuses digests with SBOMs. All represent real concepts a learner might confuse.

**explanation_accuracy**

The explanation is technically accurate. Digests are content-addressable SHA256 hashes of the manifest, and immutability is the key security property.

**difficulty_calibration**

Medium is appropriate — requires understanding of OCI internals and supply chain security, beyond a basic definition.

**domain_relevance**

Directly relevant to Cloud Native Security overview, particularly supply chain security.=

### kcsa-108

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts 'â€"' appear in option B and the explanation where em dashes should be.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. privileged: true grants all Linux capabilities, disables seccomp/AppArmor restrictions, and provides access to all host devices, effectively making the container root-equivalent on the host. The other options are unrelated to the privileged flag.

**question_clarity**

The question is clear and directly asks about the effect of privileged: true in securityContext.

**distractor_quality**

Distractors are plausible misconceptions (resource allocation, quota bypass, startup priority) and clearly wrong to an expert. Quality is acceptable.

**explanation_accuracy**

The explanation is factually accurate: privileged containers get all capabilities, bypass security restrictions, can access host devices, and are denied by PSS baseline/restricted. The note about preferring specific capabilities and CNI/storage use cases is correct.

**difficulty_calibration**

Medium is appropriate — it's a foundational security concept but requires understanding of capabilities, namespaces, and PSS.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (container security, privilege escalation).

### kcsa-138

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Workload Identity (GKE), IRSA (EKS), and Azure AD Workload Identity all map Kubernetes ServiceAccounts to cloud IAM roles via OIDC token exchange, eliminating the need to store static cloud credentials in Secrets.

**question_clarity**

The question clearly asks for both the definition of Workload Identity and the security rationale. It is unambiguous.

**distractor_quality**

Distractors A, C, and D are plausible-sounding but clearly wrong: A misrepresents it as a CRD, C confuses it with service mesh mTLS, and D incorrectly frames it as an admission controller. Good distractors.

**explanation_accuracy**

The explanation is accurate regarding OIDC token exchange and short-lived credentials. Minor note: 'Workload Identity Federation' is associated with GCP/AKS terminology; AKS uses 'Azure AD Workload Identity', but this is a minor terminology issue. Contains a mojibake artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Medium is appropriate — requires understanding of cloud IAM integration, OIDC, and ServiceAccount token projection.

**domain_relevance**

Directly relevant to Platform Security, covering credential management and cloud identity federation.

### kcsa-129

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€"' appears in explanation where an em dash should be.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Kubernetes Goat is indeed a deliberately vulnerable Kubernetes environment created by Madhu Akula for learning offensive and defensive security techniques. Answer B is definitively correct.

**question_clarity**

The question is clear and directly asks about the project and its relevance to security learning.

**distractor_quality**

Distractors are plausible — performance testing, CNCF scoring, and CIS benchmark tools are all real categories of K8s tooling, making them reasonable wrong answers.

**explanation_accuracy**

The explanation accurately describes Kubernetes Goat and its scenarios. However, there is a mojibake artifact 'â€"' that should be an em dash.

**difficulty_calibration**

Medium is appropriate — requires familiarity with security tooling ecosystem beyond core K8s knowledge.

**domain_relevance**

Relevant to threat modeling learning, though somewhat tangentially — it's more about tooling awareness than threat model concepts. Still acceptable for the domain.

### kcsa-080

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifact 'â€”' appears in option B instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€”' with em dash '—' in option B.

**Per-dimension analysis:**

**answer_correctness**

B is correct. The --etcd-cafile flag specifies the SSL CA file used to verify the etcd server's TLS certificate when the API server connects to etcd, ensuring authenticity of the etcd endpoint. The other options misrepresent the flag (it doesn't issue certs, encrypt data at rest, or sign node certs).

**question_clarity**

The question is clear and asks both what the flag does and why it matters for security, appropriate for a hard-level question.

**distractor_quality**

Distractors are plausible: A confuses CA usage (issuing vs verifying), C conflates TLS with encryption-at-rest (a real but distinct concept controlled by --encryption-provider-config), D references node cert signing (kube-controller-manager). All are clearly wrong to experts.

**explanation_accuracy**

The explanation is factually accurate regarding MITM risk and the need for paired mTLS flags. CIS Benchmark indeed requires etcd mTLS.

**difficulty_calibration**

Hard is reasonable since it requires knowing specific apiserver flag semantics and the broader etcd mTLS context.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security (apiserver-etcd communication)..

### kcsa-018

**Issues found:**

- **WARNING** `question_clarity` — Mojibake encoding artifacts â€" appear in options A and B instead of em dashes.
- **WARNING** `explanation_accuracy` — Same mojibake encoding artifact â€" appears in explanation instead of em dash.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in options A and B and in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct per Kubernetes documentation: pods not selected by any NetworkPolicy are non-isolated and all traffic is allowed by default. This is the documented behavior.

**question_clarity**

The question is clear and unambiguous, directly asking about default pod behavior regarding NetworkPolicy selection.

**distractor_quality**

Distractors are plausible: A reflects a common misconception about default-deny, C invents a namespace-level default behavior, D suggests namespace isolation. All are clearly wrong to an expert but plausible to learners.

**explanation_accuracy**

The explanation accurately describes non-isolation behavior, the role of policyTypes, and best practice of default-deny. Technically sound.

**difficulty_calibration**

Medium is appropriate — this is a commonly misunderstood concept that requires understanding of NetworkPolicy semantics beyond basic definitions.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals, specifically NetworkPolicy behavior.- relevant to KCSA.

### kcsa-104

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears in the explanation instead of an em dash '—'.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct per Kubernetes RBAC docs. Aggregated ClusterRoles use an aggregationRule with clusterRoleSelectors to automatically include rules from other ClusterRoles matching the label selector. The controller manager combines matched rules into the aggregated role's rules field.

**question_clarity**

The question is direct and unambiguous: it asks for the definition of an aggregated ClusterRole.

**distractor_quality**

Distractors are plausible misconceptions (namespace aggregation, binding combinations, external IdP delegation) but clearly wrong to anyone familiar with RBAC aggregationRule semantics.

**explanation_accuracy**

The explanation is factually accurate and cites the standard view/edit/admin aggregation pattern and the rbac.authorization.k8s.io/aggregate-to-view label. However, it contains a mojibake artifact 'â€”' instead of an em dash.

**difficulty_calibration**

Hard is reasonable — aggregated ClusterRoles are an advanced RBAC topic typically not encountered until extending with CRDs or custom roles.

**domain_relevance**

Directly tests Kubernetes Security Fundamentals (RBAC), which is a core KCSA domain.

### kcsa-072

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in option B and explanation instead of em dashes (—).

**Fixes applied:**

- Replaced mojibake â€" with proper em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B correctly defines a software attestation as a signed, verifiable statement about a software artifact. This aligns with SLSA and in-toto definitions. The other options describe unrelated concepts.

**question_clarity**

The question is clear and directly asks for the definition of software attestation in supply chain security context.

**distractor_quality**

Distractors are plausible misconceptions: API server certificates, RBAC bindings, and OCI image formats all sound related but are distinct concepts. They are clearly wrong to experts.

**explanation_accuracy**

The explanation accurately describes attestations, their storage (OCI registries, Rekor), verification via admission controllers, and SLSA's use of them. Contains mojibake encoding artifacts (â€") that should be em dashes.

**difficulty_calibration**

Hard is reasonable given supply chain security and SLSA concepts are advanced KCSA topics, though this is more of a definition question that could arguably be medium. Acceptable as hard.

**domain_relevance**

Directly relevant to Cloud Native Security overview, particularly supply chain security.

### kcsa-132

**Issues found:**

- **WARNING** `question_clarity` — Mojibake artifact 'â€"' appears in option A instead of an em dash.
- **WARNING** `explanation_accuracy` — Mojibake artifact 'â€"' appears in the explanation instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option A and in the explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Cilium uses eBPF for identity-aware policy enforcement, L7 policies, and observability via Hubble, while Flannel is a simple L3 overlay that does not implement NetworkPolicy on its own. This aligns with official Cilium and Flannel documentation.

**question_clarity**

The question is clear: it asks for what Cilium is and how it differs from Flannel as a CNI. The stem is unambiguous.

**distractor_quality**

Distractors are plausible misconceptions: A confuses Cilium with a service mesh (though Cilium does have service mesh features now, the framing 'just a service mesh, not a CNI' is wrong), C confuses component replacements, and D is a geographic falsehood. They are clearly wrong to an expert but tempting to a novice.

**explanation_accuracy**

The explanation is accurate: Cilium's eBPF-based features (identity-based policies, L7 enforcement, WireGuard, Hubble) are real, and Flannel indeed does not enforce NetworkPolicy by itself. Good context provided.

**difficulty_calibration**

Medium is appropriate—requires comparing two CNIs and understanding NetworkPolicy enforcement implications, beyond pure recall but not deeply nuanced.

**domain_relevance**

Directly relevant to Platform Security: CNI choice and NetworkPolicy enforcement are core platform security concerns.

### kcsa-073

**Issues found:**

- **WARNING** `explanation_accuracy` — The '90 days for patches' figure is inaccurate for the Kubernetes Security Response Committee. Kubernetes SRC typically uses a much shorter embargo (around 7-14 days for distributors list, then public disclosure). The 90-day window is associated with Google Project Zero, not Kubernetes. Additionally, the committee was renamed from PSC to Security Response Committee (SRC).

**Fixes applied:**

- Removed inaccurate '90 days' embargo claim and replaced with accurate description of Kubernetes embargo process
- Noted the rename to Security Response Committee (SRC)

**Per-dimension analysis:**

**answer_correctness**

Option B accurately describes the Kubernetes Security Response Committee (formerly Product Security Committee/Team). They receive reports at security@kubernetes.io, triage, coordinate fixes, and manage disclosure. The other options describe unrelated functions (RBAC config, distribution certification, CA rotation).

**question_clarity**

The question is clear and direct, asking about the function of the PSC.

**distractor_quality**

All distractors are plausible Kubernetes security-related activities but clearly wrong to someone familiar with the PSC's actual charter. They test whether the candidate confuses the PSC with CNCF conformance programs or operational security tasks.

**explanation_accuracy**

The explanation is accurate regarding the PSC's role. Minor note: the group was renamed to Security Response Committee (SRC), and the embargo period is typically up to 14 days for high-severity issues, not 90 days—though disclosure timelines can vary. The 90-day figure is more associated with Project Zero than Kubernetes SRC, which uses shorter embargo windows.

**difficulty_calibration**

Medium is appropriate; knowledge of the PSC/SRC is beyond basic but commonly tested in KCSA.

**domain_relevance**

Directly relevant to Cloud Native Security overview, covering the security response ecosystem of Kubernetes.

### kcsa-061

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifacts (â€") appear in both the option B text and the explanation where em dashes should be.

**Fixes applied:**

- Replaced mojibake â€" with em dash — in option B and explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is definitively correct. Shift-left security means integrating security earlier in the SDLC (design, code, CI), which aligns with DevSecOps principles and official cloud native security guidance.

**question_clarity**

The question is clear and direct, asking for the definition of a well-known term.

**distractor_quality**

Distractors are plausible: A inverts the concept, C is a common misconception about responsibility, D is a believable-sounding technical red herring. All are clearly wrong to someone who knows the term.

**explanation_accuracy**

The explanation is factually accurate, citing real tools (SAST, checkov, kube-linter) and correctly framing this as a DevSecOps principle. Only issue is mojibake encoding artifacts (â€") in place of em dashes.

**difficulty_calibration**

Easy is appropriate — this is a single-concept definitional question about a widely known term.

**domain_relevance**

Directly relevant to Overview of Cloud Native Security domain, covering DevSecOps fundamentals.

### kcsa-067

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in explanation.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. Distroless images contain only the application and its runtime dependencies, omitting shells, package managers, and other OS utilities. Option D describes 'scratch' images which is a related but distinct concept (scratch is empty; distroless has minimal runtime files like CA certs, tzdata, libc).

**question_clarity**

The question is clear and two-part: defines distroless and asks for security benefit. Well-phrased.

**distractor_quality**

Distractors are plausible. D is particularly good because scratch images are often confused with distroless. A and C are weaker but reasonable for novices.

**explanation_accuracy**

Explanation is accurate. Contains a mojibake artifact 'â€”' which should be an em dash. Content-wise, accurately describes distroless and contrasts with Alpine/Ubuntu.

**difficulty_calibration**

Medium is appropriate—requires knowing the specific concept beyond basic container knowledge.

**domain_relevance**

Directly relevant to Cloud Native Security overview, addressing minimal attack surface principles.

### kcsa-030

**Issues found:**

- **WARNING** `explanation_accuracy` — The explanation states Falco monitors syscalls 'using eBPF/seccomp'. Falco uses a kernel module or eBPF probe for syscall capture; seccomp is a syscall filtering/enforcement mechanism, not a monitoring technology used by Falco.

**Fixes applied:**

- Corrected technical description of how Falco captures syscalls (kernel module or eBPF, not seccomp).

**Per-dimension analysis:**

**answer_correctness**

Answer C is definitively correct. Falco and similar runtime security tools monitor syscalls to detect abnormal container behavior at runtime, which is exactly what the question asks. The other options operate at different layers (pre-deployment, API-level, admission-time) and cannot detect in-container runtime anomalies.

**question_clarity**

The question is clear and well-scoped: it asks for the MOST effective method to detect abnormal container behavior at runtime. The use of 'MOST effective' and 'at runtime' properly narrows the answer.

**distractor_quality**

All distractors are plausible security controls that learners might confuse with runtime detection. Image scanning, audit logs, and PSS are all legitimate Kubernetes security controls but operate at different phases, making them effective distractors.

**explanation_accuracy**

The explanation accurately describes Falco's function and correctly distinguishes why each other option falls short. Minor note: Falco primarily uses a kernel module or eBPF probe for syscall monitoring (not seccomp, which is an enforcement mechanism), but this is a small imprecision rather than a major error.

**difficulty_calibration**

Medium is appropriate. The learner must distinguish between four legitimate security controls and understand which operates at runtime versus other phases.

**domain_relevance**

Directly tests the Kubernetes Threat Model domain, specifically runtime threat detection and the appropriate tooling/controls.

### kcsa-103

**Issues found:**

- **WARNING** `question_clarity` — Option A contains a mojibake encoding artifact 'â€"' instead of an em dash.

**Fixes applied:**

- Replaced mojibake 'â€"' with em dash '—' in option A.

**Per-dimension analysis:**

**answer_correctness**

Answer B is correct. The fsGroup field specifies a supplemental group that applies to all containers in the pod and is used as the group ownership for volumes that support ownership management, enabling non-root containers to access mounted volumes.

**question_clarity**

The question is clear, asking both what fsGroup is and its security purpose. Option A contains a mojibake artifact (â€") but it does not affect understanding of the question.

**distractor_quality**

Distractors are plausible: A confuses fsGroup with runAsGroup, C is a reasonable-sounding wrong concept, and D plays on the 'fs' prefix. All are clearly distinguishable to someone who knows the topic.

**explanation_accuracy**

The explanation is accurate per Kubernetes documentation: fsGroup is applied as supplemental GID and used for chown/chmod on supported volume mounts (with default fsGroupChangePolicy).

**difficulty_calibration**

Medium is appropriate — this requires understanding pod securityContext beyond basic concepts but isn't a deep edge case.

**domain_relevance**

Directly relevant to Kubernetes Security Fundamentals, covering pod securityContext fields.

### kcsa-071

**Issues found:**

- **WARNING** `explanation_accuracy` — Encoding artifact 'â€”' appears in the explanation instead of an em dash (—).
- **WARNING** `difficulty_calibration` — The question tests a single conceptual fact about Vault's role, which is better calibrated as 'medium' rather than 'hard'.

**Fixes applied:**

- Replaced mojibake 'â€”' with em dash '—' in the explanation.
- Adjusted difficulty from 'hard' to 'medium' to better match the conceptual, single-fact nature of the question.

**Per-dimension analysis:**

**answer_correctness**

Option B accurately describes HashiCorp Vault's role as an external secrets manager that dynamically generates and rotates credentials. This aligns with Vault's documented capabilities including dynamic secrets, secret leasing, and the Kubernetes Auth method. The other options are factually incorrect descriptions of Vault.

**question_clarity**

The question is clear and directly asks about Vault's role in Kubernetes security architecture. No ambiguity exists.

**distractor_quality**

Distractors A, C, and D are plausible to someone unfamiliar with Vault but clearly wrong to experts. They reflect common misconceptions (replacing etcd, being an admission controller, replacing RBAC) and are well-constructed.

**explanation_accuracy**

The explanation is technically accurate regarding Vault's dynamic credentials, Kubernetes Auth method, and ServiceAccount token usage. However, it contains a mojibake encoding artifact 'â€”' that should be an em dash.

**difficulty_calibration**

Labeling this as 'hard' is questionable — it's a fairly straightforward conceptual question about Vault's role, more appropriate for medium difficulty. However, given KCSA's scope, this is a minor calibration concern.

**domain_relevance**

The question fits the 'Overview of Cloud Native Security' domain by addressing external secret management, a key cloud native security concern.
