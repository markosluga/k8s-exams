# LLM Assessment Report

**Date:** 2026-05-14 16:14:13  
**Model:** claude-opus-4-7  
**Result:** FAIL  

## Summary

| Status | Count |
|--------|-------|
| Clean — `llm_validated=1` | 3 |
| Fixed inline — `llm_validated=1` (report is audit trail) | 2 |
| Needs manual review — `llm_validated=2` | 0 |
| API failures — `llm_validated=0` (retryable) | 0 |
| **Total assessed** | **5** |

Errors: 2  Warnings: 1

## Clean Questions (llm_validated=1)

### kcsa-010

**Per-dimension analysis:**

**answer_correctness**

Option B (PodSecurity) is definitively correct. The PodSecurity admission controller went stable in Kubernetes 1.25 and is the official mechanism for enforcing Pod Security Standards via namespace labels. PSP was removed in 1.25, SecurityContextDeny is a different/deprecated controller that denies pods using SecurityContext, and AlwaysDeny is deprecated and denies all requests.

**question_clarity**

The question is concise, grammatically correct, and unambiguous. It clearly asks which admission controller enforces Pod Security Standards.

**distractor_quality**

Distractors are well-chosen: PSP is the historical predecessor (plausible to those unaware it was removed), SecurityContextDeny sounds related to pod security, and AlwaysDeny is a real admission controller name. All are clearly wrong to an expert but plausible to a learner.

**explanation_accuracy**

The explanation is factually accurate: PodSecurity went stable in 1.25, it enforces the three Pod Security Standards levels via namespace labels, and PSP was indeed removed in 1.25.

**difficulty_calibration**

Medium is appropriate. It requires knowing the PSP-to-PodSecurity transition and current Kubernetes security architecture, which is beyond a beginner definition but not deeply nuanced.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically admission control for pod security enforcement — a core KCSA topic.

### kcsa-009

**Per-dimension analysis:**

**answer_correctness**

Answer A is correct. The kubelet read-only port 10255 serves unauthenticated HTTP read-only access to endpoints like /pods, /metrics, /stats, exposing pod and node information without authentication. This aligns with Kubernetes documentation and CIS Benchmark guidance.

**question_clarity**

The question is clear and unambiguous, asking specifically about the primary security concern of the read-only port with the correct default port number cited.

**distractor_quality**

Distractors are plausible: B (RCE) is wrong because the port is read-only, C (TLS bypass for all comms) is wrong since only the read-only port lacks TLS, and D (cluster-admin) overstates the impact. All are tempting to someone unfamiliar but clearly wrong to experts.

**explanation_accuracy**

The explanation is accurate: port 10255 exposes metadata unauthenticated, the mitigation is --read-only-port=0, and CIS Benchmark (control 4.2.4) flags this. Port 10250 details are correct.

**difficulty_calibration**

Hard may be slightly aggressive; this is arguably medium since it tests a specific known port and concept. However, knowledge of kubelet port hardening and CIS controls is reasonably advanced, so hard is defensible.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security (kubelet hardening), which is a core KCSA domain.

### kcsa-007

**Per-dimension analysis:**

**answer_correctness**

Option B (--authorization-mode=RBAC) is the correct flag to enable RBAC on kube-apiserver, per Kubernetes documentation. The flag accepts a comma-separated list and RBAC is a valid mode value. Other options are factually wrong: ABAC is a different authorizer, --enable-rbac doesn't exist, and RBAC is not an admission controller.

**question_clarity**

The question is clear and direct, asking which specific flag enables RBAC. No ambiguity in phrasing.

**distractor_quality**

Distractors are well-crafted: ABAC is a real alternative authorization mode, --enable-rbac=true sounds plausible to a beginner, and --admission-control=RBAC plays on common confusion between authorization and admission control.

**explanation_accuracy**

The explanation correctly notes the typical Node,RBAC combined value and correctly characterizes ABAC as file-based and legacy. Accurate and adds useful context.

**difficulty_calibration**

Medium is appropriate — this requires knowing both the specific flag name and distinguishing authorization modes from admission controllers, which is beyond pure recall but not deeply nuanced.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security, specifically kube-apiserver configuration for authorization, which is squarely in the KCSA domain.

## Fixed Inline (llm_validated=1)

### kcsa-006

**Issues found:**

- **ERROR** `answer_correctness` — Sigstore (Cosign) is a CNCF incubating project, not a graduated project. The question's premise is factually incorrect.
- **ERROR** `explanation_accuracy` — Explanation repeats the inaccurate 'CNCF graduated' status for Cosign/Sigstore.

**Fixes applied:**

- Replaced 'CNCF-graduated' with 'CNCF (Sigstore)' in the question stem to avoid the factual error about graduation status.
- Updated explanation to correctly describe Sigstore as a CNCF incubating project.

**Per-dimension analysis:**

**answer_correctness**

Cosign is indeed the well-known tool for container image signing/verification, so B is the intended best answer among the options. However, the question premise is factually wrong: Sigstore (which includes Cosign) is a CNCF **incubating** project, not graduated. None of the listed tools is actually a CNCF-graduated image signing tool, which undermines correctness.

**question_clarity**

The question is grammatically clear and unambiguous in what it asks. The phrasing 'CNCF-graduated project for container image signing and verification' is specific.

**distractor_quality**

Distractors are well-chosen and plausible within cloud-native security: Falco (runtime), Trivy (scanning), OPA Gatekeeper (policy). Each is a real CNCF project with a distinct purpose, making them strong distractors.

**explanation_accuracy**

The explanation correctly describes Cosign/Sigstore's function (keyless OIDC signing, OCI registry storage) and accurately characterizes the other tools. However, it repeats the inaccurate claim that Cosign is a CNCF graduated tool — Sigstore is incubating, not graduated.

**difficulty_calibration**

Medium is appropriate; this requires knowing both the tool's function and its CNCF status, which is more than entry-level recall.

**domain_relevance**

Directly relevant to 'Overview of Cloud Native Security' — supply chain security and image signing are core KCSA topics.

### kcsa-008

**Issues found:**

- **WARNING** `explanation_accuracy` — Mojibake encoding artifact 'â€”' appears instead of an em dash in the explanation.

**Fixes applied:**

- Replaced mojibake 'â€”' with proper em dash '—' in the explanation.

**Per-dimension analysis:**

**answer_correctness**

etcd (C) is definitively correct — it is the key-value store backing all Kubernetes cluster state and is the component encryption-at-rest applies to. The other components (scheduler, controller-manager, kubelet) do not store cluster state.

**question_clarity**

The question is clear and unambiguous, asking specifically about the component storing cluster state that needs encryption at rest.

**distractor_quality**

Distractors are all legitimate Kubernetes control plane / node components, making them plausible to novices but clearly wrong to anyone who knows the architecture.

**explanation_accuracy**

The explanation is factually accurate: etcd holds cluster state including Secrets, and EncryptionConfiguration is configured via --encryption-provider-config on the API server. Only issue is a mojibake artifact 'â€”' instead of an em dash.

**difficulty_calibration**

Medium is reasonable; it requires knowing both the role of etcd and the encryption-at-rest concept, slightly beyond pure recall but not deeply scenario-based. Easy could also be defensible but medium is acceptable.

**domain_relevance**

Directly tests Kubernetes Cluster Component Security — etcd protection is a core KCSA topic.
